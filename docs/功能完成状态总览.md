# 功能完成状态总览

> **核对时间**：2026-09-17
> **核对方式**：逐条对照**当前代码**（不是看文档写的），四份文档 + `project-group功能缺口清单.md`
> **结论**：745 项里已完成约 **90 项**，文档严重滞后于代码。

---

## 一、文档滞后警告 ⚠️

四份旧文档里以下描述**已过期**，以此文为准：

| 旧文档写的 | 实际 |
|---|---|
| 第 3 批拖拽系统「🔄 进行中」 | ✅ **已全部完成** |
| 第 4 批「待做」 | ✅ **已全部完成** |
| 第 6 批视觉规范「待做」 | ✅ **已完成两批** |
| 主窗口清单「缺失：插入竖条 / 拖拽阈值 / 分类框重排」 | ✅ **都已实现** |
| 主窗口清单「缺失：装甲块 / 三态阴影 / 145° 渐变」 | ✅ **都已实现** |
| 主窗口清单「已知问题：Rust 从未编译」 | ✅ **已解决**（cargo build 通过） |
| MCP 方案「S1 别名层待办」 | ✅ **已实现**（在 `mcp.rs`） |

---

## 二、已完成（按批次）

### 第 0 批 · 9 项数据风险 ✅

| 编号 | 内容 |
|---|---|
| #170 #531 #535 | config / 账本损坏保护：读不出 → 另存现场 → **拦住后续写入** |
| #468 #469 | 跨进程数据锁（文件锁 + 可重入） |
| #508 #510 #521 | desktop.ini 合并读写、BOM 精确判定 |
| #515 | 核实为「已正确」，**不是缺口**（只加 `+s` 不加 `+h`） |
| #101 | 跨栏拖到空白 = 换栏移动 |

### 第 1 批 · 小项 ✅

| 编号 | 内容 |
|---|---|
| #30 #31 #219 | 日志复制全部 / 清空 / 右键复制该条 |
| #26 #27 | 删页签保护（含受保护项拒绝、含卡片先确认） |
| #35 | 侧边栏键位提示 |
| #230 #231 #233 | 标签色派生色 / 圆点对比边框 / 缺失卡片红框 |
| #456 #28 | 内容浏览三类目录直达 / 复制文件名 |

### 第 2 批 · 中等模块 ✅

| 编号 | 内容 |
|---|---|
| #15 | 建链时选择链接名（含「将换绑」提示） |
| #70 | 页签拖动重排 |

### 第 3 批 · 拖拽系统 ✅（全部完成，代码已核实）

| 编号 | 内容 | 代码证据 |
|---|---|---|
| #69 | 插入位置竖条 | `.drop-before::before` / `.drop-after::after` |
| #71 | 分类框拖动重排 | `StackedGroups` 的 `onMoveTab` / `dragFrom` |
| #102 | 跨栏落点视觉区分 | `.over-link`（实线）vs 同栏（虚线） |
| #107 | 拖拽阈值 | `DRAG_THRESHOLD = 5` |
| — | 悬停自动切页签 | 600ms 延迟 + 脉冲预告 |
| #708 | 落点页签修正 | `activeTabRef`（修复移到错误页签的 bug） |

### 第 4 批 · 配置与定制 ✅

| 编号 | 内容 |
|---|---|
| #49 #578 | **目录设置分区**（此前 config 有字段、后端读了，界面无入口） |
| #51 #432 | 快捷键自定义（注册表 + 冲突检测 + 只存差异项） |

### 额外完成（原清单未单列批次）

| 项 | 内容 |
|---|---|
| #16 #17 | 链接明细展开 / 收起 + 按状态排序 + 顶部问题计数 |
| — | 设置页底部「版本 + 数据目录」 |
| #1 #4 #676 | 色盘：SV 面板 + 色相条、吸管两段式、恢复默认可微调 |

### 第 6 批 · 视觉规范 ✅（两批）

| 批 | 内容 |
|---|---|
| 一 | 145° 渐变软垫、卡片/侧边栏三态阴影、装甲块、标题整条可点、缺失红框 |
| 二 | 图标底板内凹、文字银白浮雕、圆点指示器（#126 #127） |

### MCP · S1 别名层 ✅（此前文档标为待办）

`mcp.rs` 已实现 `ALIASES` 表：

```
backup_now → backup          lock_set  → set_lock
list_agents → scan_content (补 kind=agent)
list_skills → scan_content (补 kind=skill)
create_project / create_group → create_folder
```

设计要点（注释里写了）：**只在 `tools/call` 接受，不进 `tools/list`**，
且**必须在查开关之前**归一化——否则用户关掉 `backup` 后用 `backup_now` 还能调，是个绕过口子。

---

## 三、未完成清单

> **2026-09-17 复核**：下面几项已从"未完成"移出，详见各条备注。
> 移出的原因是**逐条回到代码里核对过**，不是看文档写的。

### A 组 · 纯前端 —— **已全部完成**

| # | 项 | 状态 |
|---|---|---|
| #595 | 备份目录分离的设置入口 | ✅ 已加三个目录行（bu/bp/bg） |
| #32 | 日志条数上限可调（显示多少即复制多少） | ✅ |
| #501 | 连锁占位符插入按钮（插到光标处） | ✅ |
| #23 | 页签管理面板 | ✅ |
| #58 | 侧边栏可收起（Shift+~） | ✅ |
| 快捷键 | F1 / F7 / mod+M / mod+D / Shift+~ | ✅ 注册表 15 → 20 条，设置页分三组 |
| #54-57 #193 | 布局记忆（三栏宽 / 日志高 / 浮层高） | ✅ 窗口尺寸记忆需宿主能力，已随主窗口任务移交 |

### B 组 · 需主窗口 / 外壳配合

| # | 项 | 状态 |
|---|---|---|
| #3 | 色盘非模态常驻 | ⏸ 需面板存活 + 目标变更通知 |
| #29 | Markdown 编辑器 | ⏸ 已划归主窗口 |
| #60 | 隐藏到托盘 | ⏸ 已随 P8-3 移交主窗口 |
| #37 | MCP 自动注册到客户端 | ⏸ 依赖 stdio 模式（已就绪），待接 |

### C 组 · 后端 —— **除系统图标外已全部完成**

| # | 项 | 状态 |
|---|---|---|
| S2 | 补 3 个 MCP 工具 | ✅ **核实为已齐全**：`pick_screen_color` 有独立分支；`refresh_content` 与 `folder_icon_set_dll` 已在别名表（别名按设计不进 `tools/list`） |
| S3 | `create_folder` 指定页签 | ✅ 新增可选 `kind` + `tab_index`，建完即登记；**越界先查再建**防孤儿目录；登记逻辑抽成 `register_card` 与 `add_card` 共用 |
| S4 | stdio 模式 | ✅ `serve_stdio` 已实现且在 Tauri `Builder` 之前拉起；有 `mcp-stdio-test.mjs`（34 项）守护接线 |
| #181 #215 | 本地链接扫描兜底 | ✅ 账本缺记录时扫磁盘真实链接反查项目组；**跳转一并兜底**（否则"看得见点不动"） |
| #5-12 | 系统图标 | 🔧 已转本地开发，施工图见 `系统图标功能交接说明.md` |

### 新发现的缺口（尚未处理）

| 项 | 说明 |
|---|---|
| `close_mcp_on_exit` 是死配置项 | Rust 里**只有字段定义与 Default，全库没有任何地方读它** —— "退出时关闭 MCP"（#194 #258 #449）目前不生效。补它要钩住 Tauri 退出事件，涉及 `main.rs`（宿主文件，与并行任务冲突风险高） |

## 四、四份文档的状态

| 文档 | 状态 | 需要更新什么 |
|---|---|---|
| `project-group-开发任务计划.md` | ⚠️ **严重滞后** | 批次状态全错（3/4/6 批标待做，实际已完成）、色盘与 Markdown 的划归没写进去 |
| `主窗口功能清单.md` | ⚠️ **滞后** | 第八节「对照原版缺失」里至少 6 项已实现；第九节「Rust 从未编译」已解决 |
| `MCP整体架构方案.md` | 🟡 部分滞后 | S1 别名层已实现（文档还标待办）；S5「编译验证」已解决 |
| `共用组件移交清单.md` | ✅ **基本准确** | 内容偏"注意事项"，不随功能进度变化，可不动 |

---

## 五、建议的下一批

A 组与 C 组（除系统图标）已清空，剩下的按「还值不值得做」排：

1. **#5-12 系统图标** —— 唯一明确未完成的后端块，已在本地推进
2. **`close_mcp_on_exit` 接上退出事件** —— 目前是死配置项，但要动 `main.rs`
   （宿主文件），建议与主窗口任务排期对齐后再做
3. **#3 色盘非模态常驻 / #37 MCP 自动注册** —— 都依赖外壳或客户端侧配合

> 说明：本文档此前把 S2 / S4 列为未完成，实际都已实现；
> S3 的措辞（"create_folder 缺 tab_index"）也不准确 ——
> 真正的问题是"建完不登记且描述里没说"。核对方式见第六节。

## 六、这份文档是怎么核对的

逐条回到**代码**里找证据，不看文档写的。几个具体手法：

- 命令数 / MCP 工具数：跑 `nexus-panel/cross-end-check.mjs`（比对命令注册与前端调用）
- 某函数是否被读到：`grep` 全库看引用数（"只有字段定义 + Default" = 死配置）
- 某字段有没有界面：扫 `FpxConfig` 全部字段，逐个查在 UI 代码里是否出现
- 测试：`node plugins/project-group/<name>-test.mjs`（零依赖，不用装东西）

