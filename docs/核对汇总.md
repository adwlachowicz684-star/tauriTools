# 745 项全量核对 · 汇总

> 核对完成时间：2026-09-17
> 方法：逐项回代码核对，**不采信清单的"现状"列**
> 明细见 `核对状态表.md`（分九批）

---

## 一、总账

| 状态 | 数量 | 占比 |
|---|---|---|
| ✅ 已完成 | **221** | 30% |
| ⬜ 待做 | **282** | 38% |
| ❓ 需确认（多为实现细节、无法静态判定） | 142 | 19% |
| ➖ 不适用 / 已有 / 记录性 | 89 | 12% |
| ↗ 移交（主窗口 / 共享插件 / 系统图标） | 16 | 2% |

（第九批 255 项为按章节估算，误差约 ±20）

**真实缺口 = ⬜ 282 + ❓ 里约一半 ≈ 350 项**。
比我上轮报的"~545"少，因为核对后发现一批"清单说没有、实际有"。

---

## 二、最重要的一条发现：清单"现状"列两个方向都不可信

核对中反复遇到清单与实际不符，**两个方向都有**：

### 方向一：清单说「没有」，实际有

| # | 清单写 | 实际 |
|---|---|---|
| 69/70/71 | 「**没有**」 | 竖条 / 页签拖拽 / 分框重排都已实现 |
| 170 | 「当前无 corrupt 备份（已 grep 确认）」 | `quarantine`+`guard_against_corrupt` 都在（旁边自己又标 ✅，自相矛盾） |
| 219 | 「当前无（已 grep 确认）」 | `App.tsx` 有日志右键复制 |
| 359 | 「当前无悬停切换」 | `CardGrid.tsx:142` 注释+实现都在 |
| 264-272 | 9 个工具标 ★缺失 | 5 个在 tools() 里，4 个走别名 |
| 507/508/515/521/525 | ❌ 无 | 全部已实现（合并式改 ini / 只加 +s / BOM 精确判定 / 深度倒序） |
| 578-595 | 「整页缺失 / 无入口」 | 我这十几轮全做了 |

### 方向二：清单说「已支持」，实际没有

| # | 清单写 | 实际 |
|---|---|---|
| 342 | ✅ 当前已支持（skill 名按 `_` 分层） | `content.rs` 查不到拆分逻辑 |

**结论**：清单的"现状"列是**挖掘时的推测**，不是核实结果。
以后任何"还差多少"的判断都必须回代码，不能数清单行数。

---

## 三、按主题的完成情况

| 主题 | 数量 | 完成度 | 说明 |
|---|---|---|---|
| **布局** | 40 | 高 | 栏宽/日志高/浮层高/收起侧栏 都做了；窗口尺寸移交 |
| **快捷键** | 30 | 高 | 20 条 + 自定义 + 键位提示 + 分组 |
| **连锁动作** | 30 | 高 | 确认弹窗/占位符/开发者模式/恢复默认/客户端 |
| **MCP** | 35 | 高 | tools() 25 个 + 别名层 + stdio；真缺的只有 account_only |
| **内容浏览** | 40 | 中 | SKILL.md / 三类直达 / 复制名 已做；右键菜单/虚拟层定位 缺 |
| **数据安全** | 30 | 中高 | 原子写/损坏隔离/跨进程锁/迁移安全网 都做了 |
| **链接** | 35 | 中 | 磁盘兜底/批量操作/键迁移 已做；编辑弹窗/单击编辑 缺 |
| **拖拽** | 90 | **低** | 基础可用，但死区/让位动画/贴边滚动/跨多框 都没做 |
| **图标** | 102 | **低** | 分组管理有；系统图标/EXE 提取/剪贴板/真转 ICO 缺 |
| **视觉规范** | 30 | 低 | token 化 + 派生画刷可一次性收口 |
| **编辑器** | 30 | 低 | 已接共享 md-editor；工具栏/分栏/滚动同步 缺 |

---

## 四、优先级建议

### P0 —— 真 bug（标 `[险]`）：4 项中 3 项已修，剩 DPI 一项

> #399 与 #410 经核实**不是 bug**（见状态表），#304/#411/#427 已修。
> 唯一剩下的是 #386/#542 DPI —— 属 Windows 专属 API，需本地验证。

| # | 问题 | 后果 |
|---|---|---|
| ~~**304 / 512**~~ | ~~desktop.ini 路径含空格不加引号~~ | **已修**（本轮） |
| **386 / 542** | 截图与取色未处理 DPI | 高 DPI 下截图错位、取色坐标错位 |
| ~~**399**~~ | ~~退出时无 flush 兜底~~ | **核实为不适用**：所有改动即时落盘（with_config / core_save_config 事务化），没有攒到退出再写的内存态。我上一轮列为 P0 是采信了清单，现已修正 |
| **410** | 搬家前先记原 ACL（窗口外读） | **核实为已满足**：lock_of(cfg, path) 在 LockGuard::new 摘锁**之前**求值 |
| ~~**411**~~ | ~~搬家/改名未抑制监控器~~ | **已修**（本轮） |
| ~~**427**~~ | ~~防写入档下自身写点无 WithUnlock~~ | **已修**（本轮）：`with_unlock()` + 覆盖式摘锁 |

### P1 —— 组合优化（省工）

1. **抽 `useDragSort` 内核**：拖拽 90 项变体还在增加，越晚抽越贵
2. **视觉规范一次性收口**：#230-237 + #276-298 约 20 项 = 一套 token + 派生函数
3. **内容浏览右键菜单**：#259-263 一处 5 项

### P2 —— 按主题批量推进

拖拽细节（#491-495 #695-729）→ 图标（#5-12 #76-80 #299-311）→ 视觉（#276-290）

---

## 五、需要你定的两件事

| # | 事项 | 说明 |
|---|---|---|
| **452** | `clusters` 集群概念 | 全库无 cluster。**是"有意不做"还是"漏了一整层"？** 我此前说过"已统一"，那是错的 |
| **342** | skill 名按 `_` 拆分层级 | 清单说已支持、实际没有。**要做吗？** |

另外编辑器方向你已定（先简单、后续引开源），
因此 #637-662（工具栏 18 按钮 / 分栏 / 滚动同步 / 渲染器）**建议不做**，
等换库时一并解决 —— 现在做了会白做。
