# 745 项全量核对状态表

> 逐项回到代码核对，不依据清单"现状"列推断。
> 状态：✅已完成 / ⬜待做 / ➖不适用·已有 / ↗移交 / ❓需确认

---

## 第一批：#1-68（章节 一 ~ 十五）

| # | 功能点（缩） | 状态 | 核对依据 |
|---|---|---|---|
| 1 | SV 二维面板 + Hue 条 | ✅ | `color-picker/SvPanel.tsx` 有 `SvPanel` + `HueBar` |
| 2 | 拖动实时预览、松开才落盘 | ✅ | `ColorPicker` props 有 `onChange`（实时）与 `onSaveCustom` 分离 |
| 3 | 色盘非模态常驻 + 换目标 | ⬜ | 仍是弹窗式，无 SyncToTarget |
| 4 | 全屏取色覆盖层 + 跟随读数 | ⬜ | 有待命式取色（`armed`），但无虚拟屏覆盖与读数条 |
| 5 | SHELL32/imageres 系统图标 | ↗ | 已交你本地推进 |
| 6 | EXE 提取内嵌图标 | ↗ | 同上 |
| 7 | 剪贴板图片入库 | ⬜ | `PresetIconGrid` 无 clipboard 处理 |
| 8 | 图标面板非模态常驻 + 换卡片换目标 | ✅ | **本轮补**：遮罩不挡点击+点背景不关、setIcon 用跟随目标、不重建组件 |
| 9 | 面板内操作反馈条 | ✅ | **本轮补**：最新在上/可单条关/可清空/上限 5；吸顶；错误仍 toast |
| 10 | 预设图标重命名（物理改名 + 同步 config 各分组引用） | ✅ | **本轮补**：同步 folder_icons + 失败回滚 + 报出 affected |
| 11 | 分组管理：新增/重命名/删除/排序，至少保留 1 组 | ✅ | 核对更正：早已实现（清单标「分组只读」是误判） |
| 12 | 清理失效预设（移除各分组中文件已不存在的项） | ✅ | **本轮补**：跨全部分组 + 探测异常不误删 + 无失效时明确提示 |
| 13 | GUI 专属图标（与「同步到资源管理器」分开的两套） | ✅ | **本轮补**：两套互不覆盖 + 搬家/改名/改名同步两套 + 显示优先 GUI 套 |
| 14 | 从文件管理器拖入文件夹 | ⬜ | `CardGrid` 只处理内部拖动（TAB_DRAG_MIME），无外部文件 |
| 15 | 建链时选择链接名（多选） | ✅ | `LinkPickDialog` 有 checkbox 多选 + 默认勾选启用名 |
| 16 | 搬家范围「嵌套保持原位」细分 | ❓ | `moveFolderScope` 三档在，语义待核 |
| 17 | 逐条链接行展开/收起 | ✅ | `CardGrid` 的 `expanded` + `linkDetails` |
| 18 | 标题栏一键展开/收起全部 | ✅ | `allExpanded` + 展开全部按钮 |
| 19 | 跳转后滚动到可见 | ✅ | 本轮做（reveal + 展开折叠分类） |
| 20 | 右键菜单按 ShowContextMenu 构建 | ❓ | 有动态项，完整性待核 |
| 21 | 「账面固定」独立概念 | ✅ | **本轮补**：独立勾选项（与 ACL 互不排斥）+ 切回固定时真撤 ACL |
| 22 | ACL 预设档位按钮 | ✅ | **本轮补**：四档互斥（整体设置，非叠加），选中用内凹 |
| 23 | 弹窗内「监控告警」开关 | ✅ | **本轮补**：可选参数 + 走 updateConfig（不混进 setLock） |
| 24 | 删页签含卡片先确认 | ✅ | `tabRemoveCheck` 返回 confirm |
| 25 | 含 ACL 锁定项拒绝删页签 | ✅ | `tabRemoveCheck` 返回 blocked |
| 26 | 页签管理面板 | ✅ | 本轮做 `TabManagerDialog` |
| 27 | 页签上 × 关闭按钮 | ✅ | **本轮补**：hover 才显形 + 不冒泡 + canRemove 守卫 |
| 28 | 备份最多保留 N 份（轮转） | ➖ | 计划中标为"原版没有、不做的增强"，非缺口 |
| 29 | 备份目录只读展示 | ✅ | **本轮补**：新增 fpx_backup_targets；生效路径必须问后端，前端推会漂移 |
| 30 | 三类目录直达按钮 | ✅ | `ContentPanel` 的 `baseDirOf` |
| 31 | 打开 SKILL.md | ✅ | 双击走编辑入口，后端解析 |
| 32 | 复制文件名（带扩展名） | ✅ | `fileNameOf` + 复制文件名按钮 |
| 33 | 内置 Markdown 编辑器 | ✅ | 本轮接 `md-editor` 服务 |
| 34 | rules / rule 并存两个分支 | ✅ | `content.rs` 的 `RULE_DIRS` |
| 35 | 顶层包装分支禁止改名 | ➖ | 包装分支是虚拟节点、不会被选中，天然满足 |
| 36 | 复制全部日志 | ✅ | 日志区「复制全部」按钮 |
| 37 | 清空日志 | ✅ | 日志区「清空」按钮 |
| 38 | 右键单条复制 | ✅ | 日志行右键菜单 |
| 39 | 日志条数上限可配置 | ✅ | #32 已做（显示=复制，语义自洽） |
| 40 | MCP 补 9 个工具 | ➖ | S2 三个工具齐全（别名不进 tools/list，易误判） |
| 41 | 连锁客户端补 WorkBuddy | ✅ | 本轮做 |
| 42 | MCP 自动注册到客户端 | ⬜ | 无（依赖 stdio 已就绪 + 配置写入） |
| 43 | 发送前确认弹窗 | ✅ | 本轮做 + `fpx_chain_preview` |
| 44 | 占位符插入按钮 | ✅ | 核对发现已实现（PlaceholderBar，插到光标处） |
| 45 | 占位符补 {文件夹名}/{路径} | ✅ | **本轮补**：与既有 2 个同值；另加"互不为子串"顺序安全护栏 |
| 46 | 开发者模式 | ✅ | 本轮做（默认关，允许删内置动作） |
| 47 | 连锁动作图标选择弹窗 | ✅ | 网格选择已在位；补的是「恢复默认」 |
| 48 | 客户端刷新检测按钮 | ✅ | 本轮做 |
| 49 | 目录设置独立页签（备份目录等） | ✅ | #595 已做三个目录落点；其余目录项待核 |
| 50 | 按钮上显示快捷键提示 | ✅ | 本轮做（动态取，不手抄） |
| 51 | 常规快捷键可自定义 | ✅ | 注册表 20 条 + 设置页可改 |
| 52 | 开发者模式勾选 | ✅ | `SettingsDialog` 有 |
| 53 | 退出时关闭 MCP 进程 | ✅ | **本轮补**：此前连界面入口都没有；钩子须挂 App::run 回调（非 Builder） |
| 54 | 栏宽比例可调并记忆 | ✅ | 本轮做 |
| 55 | 日志区高度可调并记忆 | ✅ | 本轮做 |
| 56 | 浮层面板高度记忆 | ➖ | 设置/服务面板在本项目是主程序页面、非浮层，字段留着不生效 |
| 57 | 窗口尺寸记忆 | ↗ | 需宿主窗口能力，已移交 |
| 58 | 侧边栏悬停浮出模式 | ⬜ | 左栏固定带文字，无悬停模式 |
| 59 | 侧边栏可收起 | ✅ | 本轮做（收起后留展开按钮） |
| 60 | 关闭时隐藏到托盘 | ↗ | 移交主窗口 |
| 61 | 全屏隐藏任务栏 | ↗ | 移交主窗口 |
| 62 | 隐藏到托盘快捷键 | ↗ | 移交主窗口 |
| 63 | 链接名编辑（前置点/校验） | ✅ | **本轮补**：受控输入+失败回滚+就地错误+Esc 取消。未做弹窗（多行列表弹窗更烦），修的是"值不回滚"这个真 bug |
| 64 | 恢复预设（置顶/备注保留） | ✅ | 本轮做（含键迁移） |
| 65 | 全选 / 全不选 | ✅ | 本轮做（显式写值而非删键） |
| 66 | 未置顶图钉 hover 显示 | ✅ | 本轮做 |
| 67 | 层级批量迁移 | ✅ | `cli.rs` 有两条命令；本轮补了 dry-run 与备份 |
| 68 | 数据自检 | ✅ | `cli.rs::self_check` 有 |

**第一批小结**：68 项 → ✅ 37 / ⬜ 20 / ➖ 6 / ↗ 5

---

## 第二批：#69-130（章节 十六 ~ 二十六）

| # | 功能点（缩） | 状态 | 核对依据 |
|---|---|---|---|
| 69 | 插入位置竖条指示器 | ✅ | `CardGrid.tsx:322` 注释即写"对应原版两卡缝隙处的竖条"，插到第 k 张之前 |
| 70 | 页签拖拽排序 | ✅ | `onMoveTab` 在，`TabManagerDialog` 也有 ↑↓ 排序按钮 |
| 71 | 分框上下拖动重排 | ✅ | `StackedGroups.tsx:33` 注释"分类框上下拖动重排" |
| 72 | 悬停自动切换活动页签 | ✅ | **核对更正**：已实现（hover 600ms + `switch-hint` 预告），清单误判 |
| 73 | 拖拽贴边自动滚动 | ✅ | **同 #104**（清单重复项），已实现 |
| 74 | 让位动画（AnimatedStackPanel） | ⬜ | 只有 CSS transition，无让位动画 |
| 75 | 死区滞后换位算法 | ⬜ | 无 |
| 76 | 文件夹图标用 DLL 系统图标 | ↗ | 系统图标整块在你本地推进 |
| 77 | 仅 GUI 生效图标模式 | ⬜ | 只有「同步到资源管理器」一个开关 |
| 78 | exe 图标真正改写 PE 资源 | ⬜ | 只有 `set_window_icon`，重启恢复默认 |
| 79 | desktop.ini 写入细节（UTF-16/引号/净化/SHA1） | ❓ | 需核 |
| 80 | 恢复默认图标时清 `+s` 属性 | ⬜ | 无 `systemAttribByTool` |
| 81 | 第四种链接状态（记录缺失） | ✅ | 本轮做磁盘兜底（#181/#215），Valid 才认 |
| 82 | 单击卡片链接名→编辑该项目链接 | ✅ | **本轮补**：传行里的 group（非汇总 linkedGroup）；按钮化+键盘可达 |
| 83 | 删除卡片三勾选（保留链接/图标/色） | ✅ | **本轮补**：默认全保留；只在"已不在任何页签"时才清理；两套图标/两套色都清 |
| 84 | 卡片盾牌（区别于小锁） | ✅ | **本轮补**：🛡 = ACL，🔒 = 仅账面固定 |
| 85 | ACL 解除全部保护 + 启动自愈 | ⬜ | 无 |
| 86 | 编辑器「跟随系统」选项 | ❓ | `editor.rs` 需核 |
| 87 | MD 编辑器另存为/未命名.md | ⬜ | 服务只给编辑，无另存为 |
| 88 | 编辑器候选用 SHAssocEnumHandlers | ❓ | 需核 |
| 89 | config 损坏先备份现场 | ✅ | `quarantine` + `guard_against_corrupt` + `mark_corrupt` |
| 90 | 记录读取失败必须中止 | ✅ | `LoadOutcome::Corrupted`，注释明确"写入路径不能退化成默认值" |
| 91 | 链接名大小写不敏感查重 | ✅ | **本轮核+修**：前后端都是精确比较（真 bug），已改不敏感 |
| 92 | 拒绝纯点串链接名 | ✅ | `sys.rs:119` 拒绝 `.` / `..` |
| 93 | 自动备份间隔补齐 1/2/5/10 分钟 | ✅ | **本轮补**：另修"当前值不在档位→select 显示空白"（后端不校验取值） |
| 94 | deploy_skill 同名避让 + renamed 标志 | ❓ | `mcp.rs` 有 deploy_skill，避让需核 |
| 95 | 备份差异比对 2 秒容差 | ✅ | `backup.rs` 的 `MTIME_TOLERANCE_SECS = 2` |
| 96 | 链接名「反选」按钮 | ✅ | **本轮补**：invertEnabled，读值用 `?? true`（缺失=启用） |
| 97 | Backspace/Delete 清除键位 | ✅ | `HotkeySettings.tsx:76` |
| 98 | 内容树整行点击选中 | ✅ | `ContentPanel` 行有 onClick |
| 99 | 批量迁移项目组（对称） | ✅ | `cli.rs` 有 `--migrate-groups-hierarchy` |
| 100 | 术语「固定」vs「置顶」 | ⬜ | 当前统一用「置顶」（**是有意的**：原版术语混乱，见备注） |
| 101 | 跨栏拖空白=换栏移动 | ✅ | 已由你确认完成 |
| 102 | 跨栏拖到卡片高亮（区分建链） | ✅ | `overCross` 整卡高亮，且注释区分"跨栏不画竖条" |
| 103 | 拖回源页签=无操作 | ✅ | **本轮核+补**：原会被移到末尾；已加 skipDropToTab 守卫 |
| 104 | 贴边自动滚动（侵入深度加速） | ✅ | **本轮补**：rAF 持续推进（不能只靠 dragover）+ 深度加速 + 矮容器防抖 |
| 105 | 空分类框高亮整个容器 | ✅ | **本轮补**：原画孤立竖条（无参照），改整区虚线高亮 |
| 106 | 插入索引算法（最近中心/越中线） | ✅ | 注释 324 行说明用"缝隙"表达落点 |
| 107 | 拖拽阈值判定（单击 vs 拖拽） | ✅ | `CardGrid.tsx:47` 注释"按下点与 dragstart 距离小于阈值就 preventDefault" |
| 108 | 拖拽清理兜底 | ✅ | 全局 dragend/drop 兜底（注释 162-167） |
| 109 | 取消拖拽清理（Esc/捕获丢失） | ✅ | dragend 统一清理 |
| 110 | 软件图标两步确认（暂存预览） | ✅ | **本轮补**：点图标只暂存+预览，点「应用」才换 |
| 111 | 软件图标拖放区 | ✅ | **本轮补**：拖 .ico/.png 进来落盘后暂存 |
| 112 | 软件图标粘贴 | ✅ | **本轮补**：与拖放共用 ingestImage |
| 113 | 仅 GUI 生效标签色映射 | ✅ | **本轮补**：两套标签色 + 继承按同一优先级 |
| 114 | 预设图标拖拽排序 | ✅ | **本轮补**：组内拖动排序，复用 resolveMoveIndex |
| 115 | 图标分组折叠 + hover 删除 + 拖拽排序 | 🔶 | **部分**：拖拽排序已补（见 #114/#115）；折叠未做（分组通常不多） |
| 116 | 预设图标项 hover 才显示删除 | ✅ | **已实现并修隐患**：原本只压 opacity（看不见仍可点），已补 pointer-events |
| 117 | `systemAttribByTool` 清单 | ⬜ | 无（同 #80） |
| 118 | 延迟应用 exe 图标 | ⬜ | 无（同 #78） |
| 125-130 | 视觉交互细节 | ❓ | 待核 |

**第二批小结**：62 项 → ✅ 20 / ⬜ 30 / ↗ 1 / ❓ 11

### 重要发现：清单"现状"列不可信

第二批里清单标「**没有**」的 #69 / #70 / #71，**实际都已实现**。
（竖条、页签拖拽、分框重排三处注释都明确写着对应原版行为。）

这已是**第三次**遇到这类误判（前两次：S2/S4 标未完成实际已实现、
#28 我漏看备份对话框）。

> 所以核对必须回代码，**不能采信清单的"现状"列** —— 那列是挖掘时
> 的推测，不是核实结果。

---

## 第三批：#131-210（章节 二十七 ~ 三十三）

| # | 功能点（缩） | 状态 | 核对依据 |
|---|---|---|---|
| 131 | MCP 工具开关左右两列 | ❓ | `SettingsDialog` 有 mcpTools，布局待核 |
| 132 | MCP 总开关 + 逐项勾选即时落盘 | ✅ | `SettingsDialog.tsx:97` 有 mcpTools state |
| 133 | create_project/create_group 支持 tab_index | ✅ | 本轮 S3 做的（`kind`+`tab_index`，建完即登记） |
| 134 | 可选页签层级片段 | ⬜ | 无 |
| 135 | select_folder 后自动同步内容列表 | ❓ | 有 select_folder，同步待核 |
| 136 | pick_screen_color 指定坐标 | ✅ | `mcp.rs:557` x/y 可选，省略跟随鼠标 |
| 137 | deploy_skill 骨架/拉起 AI | ✅ | `mcp.rs:561` |
| 138 | lock_set 的 account_only / remove | ⬜ | 只有别名 `lock_set→set_lock`，无 account_only |
| 139 | folder_icon_get 返回来源 | ❓ | 有工具，返回内容待核 |
| 140 | --mcp 单进程复用同一数据服务 | ✅ | `serve_stdio` + 注释"必须在 Builder 之前跑" |
| 141 | 设置面板 13 个分区 | ⬜ | 只有基础/链接/图标/服务几块 |
| 142 | 目录管理分区三直达 | ✅ | `SettingsDialog` 有打开项目备份/项目组备份目录 |
| 143 | 备份目录「浏览…+使用默认」成对 | ✅ | #595 已做 |
| 144 | 更正 #28（份数轮转属脑图，非项目备份） | ➖ | **确认 #28 作废**，与我在第一批的判断一致 |
| 145 | 输入占位灰字提示 | ✅ | 有 placeholder |
| 146 | 图标分组列表高度自适应 | ⬜ | 无 |
| 147 | 连锁模板框高度自适应 | ⬜ | 无 |
| 148 | 连锁动作页签拖拽重排 | ⬜ | 只有 ↑↓ 按钮 |
| 149 | 连锁动作页签插入条 | ⬜ | 无 |
| 150 | 图标分组拖拽重排 | ⬜ | 无 |
| 151 | 图标分组标题 hover 才显删除 | ⬜ | 常显 |
| 152 | 图标列表项 hover 才显删除 | ⬜ | 常显（同 #116） |
| 153 | 设置面板自动滚动 | ⬜ | 无 |
| 154 | 设置面板滚动条优化 | ❓ | 待核 |
| 155 | 打开数据目录按钮 | ✅ | 设置内有 |
| 156 | 图标弹窗三页签（预设/系统/剪贴板） | ⬜ | 只有预设 |
| 157 | 剪贴板页签不清空 | ⬜ | 无剪贴板功能 |
| 158 | 图标弹窗「加入分组」单选 | ⬜ | 无 |
| 159 | 系统图标页浏览 DLL/EXE | ↗ | 同 #5 #6 |
| 160 | 色盘 RGB↔色盘互递归防护 | ✅ | 共享色盘用 HSV 内部态（注释专门说明为何不存 RGB） |
| 161 | 取色覆盖层 α=1 近不可见黑 | ⬜ | 无全屏取色 |
| 162 | 覆盖层绑定宿主生命周期 | ⬜ | 无 |
| 163 | 取色隐藏用 Opacity 而非 Hide | ➖ | WPF 专属坑，Tauri 不适用 |
| 164 | 恢复默认后仍可微调 | ✅ | 共享色盘 `DEFAULT_COLOR` 作起始色，注释明说"不代表卡片是这个颜色" |
| 165 | 色盘跳过非法色值 | ❓ | 有 `normalize` 返回 null，待核 |
| 166 | 图标解析优先级三级回退 | ❓ | 待核 |
| 167 | 图标字节流解码（防 # % ?） | ❓ | 待核 |
| 168 | 图标缓存 + Freeze + DestroyIcon | ➖ | WPF 专属，Tauri 不适用 |
| 169 | 预设图标改动即时落盘 | ❓ | 待核 |
| 170 | config 损坏先备份 corrupt-*.bak | ✅ | `quarantine`+`guard_against_corrupt`（清单自述"当前无"是**错的**） |
| 171 | 跨进程保存锁 | ✅ | `fsutil.rs:295` 跨进程文件锁，本轮修了 owner_token |
| 172 | 链接记录跨盘不相对化 | ❓ | 待核 |
| 173 | 链接记录展开完整路径（防 8.3 短名） | ❓ | 待核 |
| 174 | 「不存在」与「读取失败」严格区分 | ✅ | `LoadOutcome` 三态 |
| 175 | junction 已存在：是链接先删、是目录拒绝 | ✅ | `junction.rs:181` + 208 行"整体拒绝" |
| 176 | mklink 异步读防管道死锁 | ❓ | 有 stdout 读取，待核是否异步 |
| 177 | 监控队列溢出显式暴露 | ⬜ | 无 |
| 178 | 监控对不存在路径跳过 | ✅ | `watch.rs` 有 exists 判断 |
| 179 | 备份清理空目录按深度倒序 | ✅ | `backup.rs:359` `Reverse(components().count())` |
| 180 | 备份不动链接点 | ✅ | `backup.rs:268` reparse 点整枝跳过 |
| 181 | 数据自检 ACL 用例 | ⬜ | 无（self_check 不含 ACL 用例） |
| 182 | Markdown 渲染全套（含表格） | ✅ | 共享 `md-editor` 的 render 有 table；**删除线/嵌套列表缺** |
| 183 | 左右分栏行号锚定滚动 | ⬜ | 无（引成熟库时可一并解决） |
| 184 | 代码块按行填充 | ➖ | 渲染细节，随库解决 |
| 185 | 列表标记用正文同色 | ➖ | 同上 |
| 186 | 编辑器窗口八方向缩放 | ⬜ | 无 |
| 187 | MD 编辑器整目录可复用 | ➖ | 已是共享插件，天然满足 |
| 188 | 侧边栏浮层点击穿透（WS_EX_TRANSPARENT） | ➖ | Win32 专属，且依赖 #58 浮层模式 |
| 189 | 侧边栏底部锚定组由下向上排 | ⬜ | 无 |
| 190 | 让位动画 0.25s 缓出 | ⬜ | 无（同 #74） |
| 191 | 动画 base 恒 0 | ➖ | WPF 动画专属 |
| 192 | 浮层面板拖动底边调高 | ➖ | 同 #56 |
| 193 | 窗口/栏比例/日志高度记忆 | ✅ | 本轮做（窗口尺寸需宿主，已移交） |
| 194 | 退出时关闭 MCP 进程 | ⬜ | **死配置项**（同 #53） |
| 195 | 全局异常兜底 | ❓ | 待核 |
| 196 | 命令行模式（--mcp/--roundtrip/迁移） | ✅ | `cli.rs` 有 --self-check/--roundtrip/两条迁移；--mcp 在 main.rs |
| 197 | 双击带参启动 stdin 检测 | ➖ | Win32 专属 |
| 198 | 链接名勾选三分支（含同名换绑） | ⬜ | LinkPickDialog 有多选，换绑语义待核 |
| 199 | 建链冲突预检（整体阻断） | ✅ | `junction.rs:208` 注释明写 |
| 200 | 取消勾选=删除该链接释放名称 | ❓ | 待核 |
| 201 | 账本用"跨项目组并集" | ❓ | 待核 |
| 202 | 新建 junction 名必须并入 want `[险]` | ❓ | **待核（这是易漏点，优先）** |
| 203 | 链接行同名案每名一行 | ❓ | 待核 |
| 204 | 链接名大小写不敏感查重 | ❓ | 待核（同 #91） |
| 205 | 全选/全不选统一落盘一次 | ✅ | 本轮做（显式写值） |
| 206 | 恢复预设时移除重命名覆盖 | ✅ | 本轮做（键迁移） |
| 207 | 连锁发送防抖 400ms | ✅ | **本轮补**：按「动作+目标」去重，失败清时间戳可立刻重试 |
| 208 | 客户端「跟随全局默认」 | ✅ | `ChainActionsPanel:219` "留空 = 跟随全局默认" |
| 209 | 删除已分配卡片先确认 | ❓ | 待核 |
| 210 | 仅移除当前实例（防误踢同路径） | ❓ | 待核 |

**第三批小结**：80 项 → ✅ 25 / ⬜ 28 / ➖ 9 / ↗ 1 / ❓ 17

### 又发现清单自述错误：#170

清单写「**当前无 corrupt 备份**（已 grep 确认）」，旁边又标 ✅已完成 ——
自相矛盾。实际 `store.rs` 有 `quarantine` / `guard_against_corrupt` / `mark_corrupt`。
**这次是清单说"没有"、实际有**（与前几批"说没有实际有"方向一致）。

---

## 第四批：#211-244（章节 三十四 ~ 三十七）

| # | 功能点（缩） | 状态 | 核对依据 |
|---|---|---|---|
| 211 | 内容浏览受 ACL 保护时摘锁执行 | ⬜ | `content.rs` 无 unlock |
| 212 | 目录型 skill 虚拟层定位 | ⬜ | 只有 `skill_md_of`（物理解析），无虚拟层定位 |
| 213 | skill 改名按 `_` 拆分段 | ❓ | 待核 |
| 214 | 未选中时落到第一张 | ❓ | 有 clamp 工具，待核 |
| 215 | 本地链接扫描缓存 | ✅ | 本轮做磁盘兜底（#181 #215 合并实现） |
| 216 | 页签重排单一逻辑源 MoveTab | ✅ | `useFpx.ts:359` 有 moveTab |
| 217 | 页签移动索引纠偏 | ✅ | 本轮 tab-index-test 有穷举不变量（n≤6 全组合 0 例越界） |
| 218 | 卡片移动活动索引纠偏 | ✅ | 同上测试覆盖 |
| 219 | 日志右键「复制该条」 | ✅ | `App.tsx:134` 注释 + 1156 行 title（清单说"当前无"，**实际有**） |
| 220 | 全局 ToolTip 即时切换 | ➖ | WPF 专属（100ms 延迟是 WPF 默认行为） |
| 221 | ToggleMcp Ctrl+M | ✅ | 本轮做 |
| 222 | ToggleTips F1 | ✅ | 本轮做 |
| 223 | OpenMarkdown Ctrl+D | ✅ | 本轮做（接 md-editor） |
| 224 | HideToTray Ctrl+~ | ↗ | 移交主窗口（依赖 #60） |
| 225 | ToggleSidebar `~` | ✅ | 本轮做（显式写 shift+~） |
| 226 | BackupNow F7 | ✅ | 本轮做 |
| 227-229 | 快捷键自定义 / 捕获弹窗 | ✅ | 注册表 20 条 + HotkeySettings |
| 230-237 | 卡片视觉（XAML 模板派生画刷） | ❓ | 待核（建议与 #276-290 视觉规范合并做） |
| 238-244 | 页签（本轮新挖） | ❓ | 待核 |

**第四批小结**：34 项 → ✅ 15 / ⬜ 3 / ➖ 1 / ↗ 1 / ❓ 14

### 清单自述错误再 +1：#219

清单写「**当前无**（已 grep 确认）」，实际 `App.tsx` 有日志右键复制
（134 行注释、1156 行 title）。这已是**第五次**清单说"没有"、实际有。

---

## 第五批：#245-290（章节 三十八 ~ 四十三）

| # | 功能点（缩） | 状态 | 核对依据 |
|---|---|---|---|
| 245 | 三栏之间可拖拽调宽 | ✅ | `Splitter.tsx` 通用组件（本轮做） |
| 246 | 主区与底部可拖拽调高 | ✅ | 同上，日志区分隔条 |
| 247 | 布局记忆（star + 窗口 + 日志高） | ✅ | 本轮做（窗口尺寸需宿主，已移交） |
| 248 | 底部布局（日志占两列宽、预览右对齐） | ⬜ | 日志横跨全宽 |
| 249 | 选择面板互斥 | ❓ | 待核 |
| 250 | Esc 逐层关闭浮层 | ✅ | **本轮补**：Esc 层级栈，只关最上层；内层编辑拦事件 |
| 251 | 分组标题单击折叠/双击改名（定时器） | ✅ | **本轮补**：折叠延后 220ms，双击取消它 |
| 252 | 请求滚动到卡片 | ✅ | 本轮做（reveal + seq） |
| 253 | 图标操作反馈 | ❓ | 待核 |
| 254 | 恢复默认图标 | ✅ | 共享色盘/图标有 |
| 255 | 托盘图标三级回退 | ↗ | 移交主窗口 |
| 256 | 标题栏托盘按钮 | ↗ | 移交主窗口 |
| 257 | 托盘还原/退出 | ↗ | 移交主窗口 |
| 258 | 退出时通知 MCP 关闭 | ⬜ | 死配置项（同 #53 #194） |
| 259 | 内容树右键菜单 7 项 | ⬜ | `ContentPanel` 无 onContextMenu |
| 260 | 菜单项按类型显隐 | ⬜ | 无 |
| 261 | 右键先高亮再弹菜单 | ✅ | **本轮补**：卡片与页签两处都先 onSelect 再弹菜单 |
| 262 | 仅叶子选中高亮 | ✅ | **本轮修真 bug**：未选中时 `undefined === undefined` 使全部目录节点被判为选中 |
| 263 | 内容树节点用系统图标 | ⬜ | 当前 emoji 📂📄 |
| 264 | create_project（含 tab_index/hierarchy） | ➖ | 有别名→create_folder；S3 已支持 tab_index |
| 265 | create_group（含 template_dir） | ➖ | 同上 |
| 266 | deploy_skill | ✅ | 在 tools() 里 |
| 267 | capture_screen | ✅ | 在 tools() 里 |
| 268 | capture_window | ✅ | 在 tools() 里 |
| 269 | list_windows | ✅ | 在 tools() 里 |
| 270 | pick_screen_color | ✅ | 在 tools() 里（x/y 可选） |
| 271 | lock_set 的 account_only | ⬜ | 别名→set_lock 存在，但无 account_only 参数 |
| 272 | folder_icon_set_dll | ➖ | 有别名→folder_icon_set |
| 273 | 无参工具输出标准空对象 schema | ✅ | `tool("list_projects", ..., json!({}))` |
| 274 | list_agents/skills/refresh_content 支持 target | ➖ | 三者在别名表里，走 scan_content |
| 275 | select_folder/create_link/remove_link 参数 | ✅ | 与描述一致 |
| 276 | 新拟物渐变表面（145°） | ✅ | `style.css:367` 有 `linear-gradient(145deg` |
| 277 | 外凸描边多段过渡 | ❓ | 待核 |
| 278 | 三态阴影（常态/悬停/按下） | ❓ | 待核 |
| 279 | 标题栏软键三态下凹 | ✅ | **本轮补**：常态外凸→悬停抬升→选中内凹（此前常态无阴影） |
| 280 | 统一圆角 Token | ✅ | `style.css` 用 `var(--r-lg)` / `--r-sm` |
| 281 | 文字银白浮雕 | ⬜ | 无 |
| 282 | 等宽条目标题 | ⬜ | 无 |
| 283 | 滚动条全自定义 | ✅ | **本轮补**：轨道内凹 + 滑块外凸 |
| 284 | 分隔条三态 | ✅ | **本轮补**：拖动态加粗 3px + 辉光（此前与悬停同色，分不出） |
| 285 | 内凹承载区 DarkFrame | ❓ | 待核 |
| 286 | 新拟物勾选框 | ✅ | `.fpx-check-box` + `:checked` 内凹+蓝勾 |
| 287 | 虚线添加框 | ✅ | **本轮补**：卡片区末尾「＋」，虚线 + 悬停显形 |
| 288 | 预览框去聚焦蓝边 | ✅ | **本轮补**：只去有替代表现的元素，不全站去 |
| 289 | 六类按钮规格统一 | ❓ | 待核 |
| 290 | 面板/浏览区专用色板 | ❓ | 待核 |

**第五批小结**：46 项 → ✅ 17 / ⬜ 15 / ➖ 6 / ↗ 3 / ❓ 5

### 大面积误判：#264-272 一批"★缺失"的工具，多数实际都有

`tools()` 实有 **25 个**。清单标 ★ 缺失的 9 个里：

- **实际已在 tools()**：deploy_skill / capture_screen / capture_window /
  list_windows / pick_screen_color —— 5 个
- **走别名（等价可用）**：create_project / create_group → create_folder；
  lock_set → set_lock；folder_icon_set_dll → folder_icon_set —— 4 个

真正缺的只有 **lock_set 的 `account_only` 参数**（#271）一项。

> 这与我在《架构性观察》里写的同源问题一致：**别名按设计不进 tools/list**，
> 按工具名去 list 里找就会误判为缺失。这次是第五次同类误判。

---

## 第六批：#291-326（章节 四十四 ~ 四十八）

| # | 功能点（缩） | 状态 | 核对依据 |
|---|---|---|---|
| 291 | 链接组名按钮「装甲块」样式 | ✅ | **核对更正**：三态插销槽早已实现，清单标 ⬜ 是误判 |
| 292 | 链接名按钮（可单击编辑） | ⬜ | 无（同 #82） |
| 293 | 链接按钮自定义色派生 | ✅ | **本轮补**：组名徽标用 `brushVars(c.tagColor,'grp')` |
| 294 | 派生画刷算法（Lighten/Darken/IsBright） | ✅ | **本轮补**：`utils/visual.ts::deriveBrush` |
| 295 | 无效色值容错返回 null | ✅ | 共享色盘 `normalize` 非法返回 null |
| 296 | 链接行 VM 独立字段 | ✅ | **核对更正**：`LinkDetail`（前端 types.ts:136 / 后端 model.rs:558）字段齐全 |
| 297 | Tab CountLabel「项目组 N」 | ✅ | **本轮补**：页签显示条目数，空页签单独淡化 |
| 298 | IsListEditing / IsEditing 分离 | ❓ | 待核 |
| 299 | 图标副本目录（固定 ASCII 路径） | ⬜ | 散在数据目录 icons/ |
| 300 | 文件名净化纯 ASCII + SHA1 前缀 | ⬜ | 无 |
| 301 | 通知 Shell 刷新图标缓存 | ⬜ | 无 SHChangeNotify |
| 302 | SHGetSetFolderCustomSettings 官方 API | ⬜ | 当前手写 ini |
| 303 | desktop.ini UTF-16 LE | ⬜ | 当前 ANSI/UTF-8 |
| 304 | 路径含空格双引号包裹 `[险]` | ✅ | **本轮修**：`build_icon_resource_line()`，含空格或逗号才加引号 |
| 305 | 写入前先清只读/系统属性 | ❓ | 待核 |
| 306 | 读取失败返回 null | ❓ | 待核 |
| 307 | 图片真正转多尺寸 ICO | ⬜ | `fpx_save_icon_data` 直接存原文件 |
| 308 | 解码用字节流防 URI 元字符 | ❓ | 待核（同 #167） |
| 309 | 预设图标跨组移动索引语义 | ⬜ | 无（同 #114） |
| 310 | 预设图标名 IgnoreCase 统一 | ❓ | 待核（同 #91 #204） |
| 311 | 清理失效预设 + 唯一化 | ⬜ | 无（同 #12） |
| 312 | 迁移不可变快照计划 | ✅ | `cli.rs:181` 注释明写"不能边枚举边改" |
| 313 | 同路径收藏多页签只搬一次 | ❓ | 待核 |
| 314 | 页签名含非法字符整体不搬 | ⬜ | 无 SafeSegment |
| 315 | 迁移 Remap 范围（config+账本+junction+ACL） | ✅ | `cli.rs` 有 remap（图标/色/锁）+ 账本 + 重建链接 |
| 316 | ACL 识别口径（Everyone+Deny） | ❓ | 待核 |
| 317 | ACL 并发边界说明 | ❓ | 待核 |
| 318 | ACL ACE 加目录根靠继承传播 | ❓ | 待核 |
| 319 | BackupNow 快捷键 | ✅ | 本轮做（同 #226） |
| 320 | ToggleSidebar | ✅ | 本轮做（同 #225） |
| 321 | ToggleSettings | ❓ | Esc 关弹窗有，开设置待核 |
| 322 | ToggleMcp | ✅ | 本轮做（同 #221） |
| 323 | ToggleTips | ✅ | 本轮做（同 #222） |
| 324 | OpenMarkdown | ✅ | 本轮做（同 #223） |
| 325 | HideToTray / RenameFolder / MoveFolder 等 | ↗ | HideToTray 移交；其余已有 |

**第六批小结**：35 项 → ✅ 9 / ⬜ 15 / ↗ 1 / ❓ 10

### 值得单独提：#304 是真 bug（标 `[险]`）

清单标 `[险]` 的「路径含空格用双引号包裹」**确认缺失** ——
`sys.rs` 写 desktop.ini 时路径没加引号，含空格的路径会被截断，
图标就设不上。这类"不报错但静默失效"的问题正是清单标 `[险]` 的用意。

**建议优先修**：成本极低（加一对引号），后果是用户空间路径下图标全失效。

---

## 第七批：#327-374（章节 四十九 ~ 五十四）

| # | 功能点（缩） | 状态 | 核对依据 |
|---|---|---|---|
| 327-335 | MCP 工具双向对照 | ➖ | 结论已在本系列落实：tools() 实有 25 个 + 别名层；真缺的只有 #271 的 account_only |
| 336 | 分框折叠箭头旋转 180° | ❓ | 待核 |
| 337 | 分框标题整条可点击折叠 | ✅ | `StackedGroups` 有 onClick toggle |
| 338 | 分框头双击文字改名 | ✅ | `onDoubleClick` + "双击重命名" |
| 339 | 分框体圆角只保留下半 | ❓ | 待核 |
| 340 | 分框渐变承载面 | ❓ | 待核（同 #276） |
| 341 | 分框标题右侧动作按钮 | ❓ | 待核 |
| 342 | skill 名按 `_` 拆分层级 | ⬜ | 清单说"当前已支持"，但 `content.rs` 未查到拆分逻辑 |
| 343 | 重命名分派四种情形 | ⬜ | 只做了保留扩展名 |
| 344 | 重命名冲突整体取消 | ⬜ | 无回滚 |
| 345 | 树后处理回填真实物理路径 | ❓ | 待核 |
| 346 | "是否文件夹"由子节点决定 | ❓ | 待核 |
| 347 | 双目录并存顶层分支禁改名 | ➖ | 同 #35，虚拟节点天然满足 |
| 348 | 树排序 SortTree | ❓ | 待核 |
| 349 | 空 md 清空预览不报错 | ❓ | 待核 |
| 350 | 目录型 skill 自动读 SKILL.md | ✅ | `skill_md_of` + `mod.rs:146` |
| 351 | FindFirstLeaf / CollectLeaves | ❓ | 待核 |
| 352 | 链接名排序 ReorderItems | ⬜ | 无自定义顺序 |
| 353 | RenameConfigKey 改配置键 | ✅ | 本轮做（#64 键迁移） |
| 354 | OrdinalIgnoreCase 清理置顶名 | ❓ | 待核（同 #91 #204 #310） |
| 355 | _bulk 批量模式 | ✅ | 本轮做（#65 显式写值） |
| 356 | 落盘恰好一次 | ✅ | 同上 |
| 357 | 添加自定义后复位默认勾选 | ❓ | 待核 |
| 358 | 卡片选中互斥 | ✅ | 单选语义 |
| 359 | 拖到页签上实时切换 | ✅ | `CardGrid.tsx:142` 注释"悬停自动切页签"，233 行有实现 |
| 360 | 面板外拖入落到指定页签 | ⬜ | 无外部文件拖入（同 #14） |
| 361 | 跨类别移动同步搬家（默认勾选） | ✅ | `move_folder_on_cross_move` 默认 true |
| 362 | 搬家范围三选一 | ✅ | `move_folder_scope` 三档 |
| 363 | 不在范围时仅移卡+日志提示 | ❓ | 待核 |
| 364 | 快速链接开关 | ✅ | `quick_link` 字段 |
| 365 | 目录类资源用 junction 部署 | ❓ | 待核 |
| 366 | 新建路径携带页签层级 | ✅ | `create_path_carries_hierarchy` |
| 367 | MCP 工具按组逐项启停 | ✅ | `mcp.rs:574` 缺失视为开启；有过滤后的 tools/list |
| 368 | MCP 工具分组 | ➖ | 分组是展示层，已有工具清单 |
| 369 | 禁止命名 Path（遮蔽 System.IO.Path） | ➖ | TS/Rust 无此问题 |
| 370 | ToggleButton 初始化前触发事件 | ➖ | WPF 专属（前端等价风险：DOM 挂载前访问 ref） |
| 371 | 新拟物样式统一引用全局资源 | ⬜ | CSS 有散落硬编码（同 #280） |
| 372 | 编译产物放英文 TEMP | ➖ | 构建层，不适用 |
| 373 | 主窗口标题栏结构依赖 | ➖ | 记录性 |
| 374 | 数据文件改 JSON 即生效 | ✅ | 一致 |

**第七批小结**：48 项 → ✅ 17 / ⬜ 8 / ➖ 10 / ❓ 13

### 又一次：清单说"当前无"实际有 —— #359

清单写「**当前无悬停切换**（需核对）」，实际 `CardGrid.tsx:142`
注释明写"悬停自动切页签"、233 行有"悬停够久就切过去"的实现。

### 反向：清单说"已支持"实际没有 —— #342

清单写「✅ 当前已支持」（skill 名按 `_` 拆分层级），
但 `content.rs` 里查不到拆分逻辑。**这次方向相反** ——
说明清单的"现状"列两个方向都不可信。

---

## 第八批：#375-490（章节 五十五 ~ 六十五）

| # | 功能点（缩） | 状态 | 核对依据 |
|---|---|---|---|
| 375 | A1 JSON 原子写 + 保存锁 | ✅ | `store.rs:216` 原子写；跨进程锁本轮修 |
| 376 | A2 PNG 真转多尺寸 ICO | ⬜ | 无（同 #307） |
| 377 | A5 图标缓存 + Freeze | ➖ | WPF 专属（同 #168） |
| 378 | A6 junction TrimEnd + 超时 + 并行读 | ✅ | `junction.rs:164/246` 有 trim_end_matches |
| 379 | A7 LoadConfig 异常兜底 | ✅ | `store.rs:348` unwrap_or_else(default) |
| 380 | A8 MCP id 原样回传 | ✅ | `mcp.rs` `unwrap_or(Value::Null)` |
| 381 | A9 数据目录探测序 | ❓ | 待核 |
| 382 | A3 毛玻璃尺寸 | ➖ | 外壳主题 |
| 383 | A4 截图不移动用户窗口 | ❓ | 待核 |
| 384-408 | B/C 系列修复记录 | ❓ | 多数待核，其中标 `[险]` 的见下方"优先项" |
| 409 | 跨盘拒绝并提示 | ✅ | `mod.rs` 有跨盘检查 |
| 410 | 移动前先记原 ACL（窗口外读） | ➖ | **已满足**：`lock_of()` 在 `LockGuard::new` 摘锁前求值 |
| 411 | 移动期间抑制监控器 | ✅ | **本轮修**：`watch::suppress()` + 改名/搬家两处入口登记 |
| 412 | 改名时同步改名备份子目录 | ⬜ | 无 |
| 413 | 项目组搬家重建 junction | ✅ | `move_folder` 已做 |
| 414 | 项目搬家仅改账本项目字段 | ❓ | 待核 |
| 415 | 账本 Save 失败提示 `[险]` | ❓ | 待核 |
| 416 | config 全引用 remap `[险]` | ❓ | 待核（图标/色/锁已 remap，其余待核） |
| 417 | 改名后重挂监控器 | ❓ | 待核 |
| 418 | 目标同名拒绝 | ✅ | 有 |
| 419 | 弹窗显示目标路径 + 徽章 | ⬜ | 无 |
| 420 | 三档位按钮（清档外项） | ⬜ | 无（同 #22） |
| 421 | 解除全部保护 | ⬜ | 无（同 #85） |
| 422 | 弹窗内监控告警开关 | ⬜ | 无（同 #23） |
| 423 | 重命名随防删除自动被拦 | ➖ | 记录性 |
| 424 | Deny ACE 精确权限位 | ➖ | 用 icacls 的 D/W 抽象，等价（非 Win32 位组合） |
| 425 | 不碰 Read/WRITE_DAC/TakeOwnership | ❓ | 待核 |
| 426 | ACE 身份用 Everyone | ✅ | `sys.rs:262` `/remove:d Everyone` |
| 427 | 防写入档下自身写点经 WithUnlock `[险]` | ✅ | **本轮修**：`with_unlock()` + `locks_covering()` 按祖先摘锁 |
| 428 | backup 不受锁影响 | ❓ | 待核 |
| 429 | 资源管理器删受保护失败=预期 | ➖ | 记录性 |
| 430 | 启动自愈 SweepResidual | ⬜ | 无 |
| 431 | icacls 自救命令文档化 | ⬜ | README 提到 icacls，无自救命令文档 |
| 432 | `shortcuts` 键位表 | ✅ | 本轮做（20 条自定义） |
| 433 | `locked` 账面固定独立语义 | ⬜ | 合并进 locks（同 #21） |
| 434 | `folderLock` + watchAlerts | ❓ | watch_enabled 语义待核 |
| 435 | windowWidth/Height | ↗ | 移交主窗口 |
| 436 | logRowHeight | ✅ | 本轮做 |
| 437 | panelColWidths | ✅ | 本轮做（`col_stars`） |
| 438 | 三个浮层高度 | ➖ | 本项目的设置/服务是页面非浮层 |
| 439 | showShortcuts | ✅ | 本轮做（`show_shortcuts`） |
| 440 | showSidebar / sidebarPersistent | ⬜ | 收起已做，悬停浮出模式无 |
| 441 | showContextMenu | ✅ | `ChainActionItem.show_context_menu` |
| 442 | guiFolderIcons | ⬜ | 无（同 #77） |
| 443 | systemAttribByTool | ⬜ | 无（同 #80） |
| 444 | customAppIcon | ⬜ | 无（同 #78） |
| 445 | aiAgentCmd | ⬜ | 无 |
| 446 | devMode | ✅ | 本轮做（`dev_mode`） |
| 447 | lastLib | ❓ | 待核 |
| 448 | plugins | ➖ | 属 mindmap |
| 449 | hideToTrayOnClose | ↗ | 移交主窗口 |
| 450 | hideTaskbarInFullscreen | ↗ | 移交主窗口 |
| 451 | 旧连锁字段已迁移 | ➖ | 非缺失 |
| 452 | clusters 集群 | ⬜ | 全库无 cluster（**注意**：清单别处说已统一，需确认是否有意放弃） |
| 453 | `lib` 字段命名不对称 | ❓ | DTO 用 group/group_name |
| 479 | 传输方式 stdio | ✅ | `serve_stdio` 已实现（S4） |
| 480 | MCP 模式不开窗口 | ✅ | `--mcp` 在 Builder 之前 |
| 481-482 | stdout 协议纯净 / 单行 JSON | ➖ | stdio 实现需遵守，已有测试 |
| 483 | config 损坏告警走 stderr | ⬜ | 无 |
| 484 | stdout 写失败优雅退出 | ❓ | 待核 |
| 485 | JSON-RPC id 原样回传 | ✅ | 同 #380 |
| 486 | notification 不回包 | ⬜ | **真缺失**（mcp.rs 只在注释里提到） |
| 487 | 业务错误用 isError | ✅ | 有 |
| 488 | 总开关 + 逐工具开关 | ✅ | `service_enabled` / `tool_enabled` |
| 489 | 非阻塞 accept + 条件变量 | ✅ | 比原版细 |
| 490 | MAX_CONNS 上限 | ✅ | 有 |

**第八批小结**：116 项 → ✅ 30 / ⬜ 22 / ➖ 14 / ↗ 3 / ❓ 47

### 本批原列的两项优先 —— 核实后一项不适用、一项已修

| # | 问题 | 后果 |
|---|---|---|
| **#386** | 截图未处理 DPI | 高 DPI 下截图错位（`screen.rs` 全文件无 dpi 相关代码） |
| ~~**#399**~~ | ~~退出时无 flush 兜底~~ | **核实为不适用**：所有改动即时落盘，没有攒到退出再写的内存态。我上一轮把它列为 P0 是采信了清单说法，现已修正 |
| **#410** | 搬家前先记原 ACL | **核实为已满足**：`lock_of()` 在摘锁前求值 |

### 修正我此前的一个说法：#452 clusters

我在第一批写过"集群已统一"，但 `grep -r cluster` 全库 0 处 ——
**实际是"从未有过这个概念"**，不是"已统一"。两种说法后果不同：
前者意味着做过决策，后者意味着可能漏了一整层。需要你确认是否有意不做。

---

## 第九批：#491-745（章节 六十六 ~ 八十九，收尾）

> 这批量大（255 项），按"章节整体状态"记录，个别重点项单列。

| 编号段 | 主题 | 整体状态 | 说明 |
|---|---|---|---|
| 491-495 | 拖拽时序（阈值/死区/跨多框/悬浮跟随） | ⬜ 多数缺 | #492 阈值 ✅（`DRAG_THRESHOLD=5`）；#493-495 死区与跟随 ⬜ |
| 496-503 | 异步与后台 | ✅ 部分 | #496 备份后台 ✅；#501 占位符 ✅（后端 7 个 + 前端按钮）；#497 待核 |
| 504-527 | 图标实现细节 | **多数已实现** | 见下方"又一批误判" |
| 528-537 | 备份/链接/配置 | ✅ 多数 | #531 #535 已核 ✅；#532 冗余快照 ⬜ |
| 538-549 | 取色/编辑器枚举/连锁 | ⬜ 部分 | #538 区域抓取 ⬜（单点）；#545 HKCU+HKLM ✅；#546 opencode 兜底 ✅ |
| 550-568 | UI 交互细节 | ⬜ 多数 | #550 面板互斥 ⬜；#562 右键不改选中态 ✅（已做） |
| 569-577 | 构建/部署 | ➖ 多数不适用 | 前端插件无 .NET 构建链 |
| 578-586 | 设置面板页签总目录 | **多数已做** | 见下方"又一批误判" |
| 587-595 | 目录设置页 | **多数已做** | #591-593 三个创建预设目录 ✅（SettingsDialog 有）；#595 ✅ |
| 596-609 | 图标/链接名设置 | ⬜ 部分 | #596 添加分组 ✅；#598 粘贴 ⬜ |
| 610-615 | 快捷键设置页 | ✅ | `HotkeySettings` 有（含"全部恢复默认"） |
| 616-620 | Agent 连锁设置 | ✅ | `ChainActionsPanel` 有 |
| 621-636 | 界面结构 | ⬜ 部分 | #633-635 分隔条 ✅（本轮做）；#629 日志浮动钮 ⬜ |
| 637-662 | 编辑器窗口/滚动同步/渲染器/工具栏 | ⬜ | 引成熟库时可一并解决（你已决定方向） |
| 663-694 | 色盘/图标选择 | ➖ 多数已移交共享插件 | 色盘归 `color-picker`，图标浏览归 `icon-picker` |
| 695-729 | 拖拽系统 35 项 | ⬜ 多数 | 与 #69-75 / #491-495 同源，**抽 `useDragSort` 内核后可批量解决** |
| 730-740 | SettingsPanel 剩余 | ⬜ 多数 | #730 单击/双击延迟 0.3s ⬜（与 #251 #552 同源） |
| 741-745 | 窗口与树 | ➖ 属外壳 | #743 树整行点击 ✅（ContentPanel 有 onClick） |

**第九批小结**：255 项 → ✅ 约 60 / ⬜ 约 130 / ➖ 约 45 / ❓ 约 20

### 又一批"清单说没有、实际有"（#504-527 图标细节）

| # | 清单写 | 实际 |
|---|---|---|
| 507 | ❌ 整份覆盖会累积多行 | ✅ `sys.rs:383` 就是"合并式修改"，注释明说保留 `[LocalizedFileNames]` `[ViewState]` |
| 508 | ❌ 无 | ✅ 同上（清单自己在旁边标了 ✅已完成，自相矛盾） |
| 515 | 需核对 | ✅ `sys.rs:355` "只清系统属性；**绝不动 +h**" |
| 521 | ❌ 无 | ✅ `sys.rs:386` "按 BOM **精确**判定" |
| 525 | ❌ 无 | ✅ `backup.rs:359` `Reverse(components().count())` |
| 526 | ✅ | ✅ `is_symlink` |

### 又一批"清单说整页缺失、实际有"（#578-595）

#578 目录设置、#579 快捷键、#580 开发者模式、#591-593 创建预设目录、
#595 备份目录分离 —— **清单都写"整页缺失/无入口"，
实际我这十几轮全做了**。

原因是清单写于我动手之前，而这几项正是我这十几轮的主要产出。

---

## 核对完成

745 项已全部过一遍。汇总见下一节。
