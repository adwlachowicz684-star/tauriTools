# agg — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/agg.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：表格
- **node.type**：`agg`
- **源文件**：`nodes/defs/agg.ts`
- **产出**：text（文本）　**接受**：table
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

汇总出来的一个数

## 注意

- 它只接受 table，其余类型接上去会被告警。

## 面板上的提示

> 列名写错会报错而不是给 0 —— 汇总值算错往往要到最后才发现。

共 2 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `col` | text | 列名；占位：如：伤害 | — | — |
| `op` | select | 方式 | `sum` / `avg` / `min` / `max` / `count` | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'agg' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
