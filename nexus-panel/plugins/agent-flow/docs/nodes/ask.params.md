# ask — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/ask.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：控制器
- **node.type**：`ask`
- **源文件**：`nodes/defs/ask.ts`
- **产出**：text（文本）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

人填的内容

## 面板上的提示

> 运行到这里会弹框等待。取消或环境不支持会明确失败，不会静默跳过。

共 3 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `prompt` | textarea | 提示语；支持模板，可以把上游内容显示给人看；占位：如：请确认这个标题是否合适 | — | — |
| `value` | text | 预填内容；占位：可留空 | — | — |
| `required` | switch | 必填；勾上时，没填内容会算失败 | — | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'ask' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
