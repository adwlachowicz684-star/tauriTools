# canvasIn — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/canvasIn.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：流程控制
- **node.type**：`canvasIn`
- **源文件**：`nodes/defs/canvasIn.ts`
- **产出**：any（透传上游）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

外部传进来的内容（原样透传）

## 面板上的提示

> 它不做计算，只把外部传进来的数据原样交给内部下游。放了这个节点后，这张画布的入口就以它为准。

共 1 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `portName` | text | 端口名；多个入口时用它区分；不填就用节点标题；占位：如：原始数值 | — | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'canvasIn' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
