# canvasOut — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/canvasOut.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：流程控制
- **node.type**：`canvasOut`
- **源文件**：`nodes/defs/canvasOut.ts`
- **产出**：any（透传上游）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

要送出画布的内容

## 面板上的提示

> 它不做计算，只把内部结果原样送出画布。放了这个节点后，这张画布的出口就以它为准。

共 2 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `portName` | text | 端口名；多个出口时用它区分；占位：如：推导结果 | — | — |
| `fallback` | textarea | 兜底值；不填就输出空 —— 不会让画布失败；占位：内部没东西时输出这个（可留空） | — | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'canvasOut' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
