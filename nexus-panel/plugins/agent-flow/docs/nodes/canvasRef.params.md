# canvasRef — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/canvasRef.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：流程控制
- **node.type**：`canvasRef`
- **源文件**：`nodes/defs/canvasRef.ts`
- **产出**：any（透传上游）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

被调用画布的出口结果

## 面板上的提示

> 被调用的画布可以用「画布输入 / 画布输出」节点自己定接口；不放这些节点时，接口会自动识别（没有上游的当入口、没有下游的当出口）。

共 2 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `canvasId` | select | 调用哪张画布 | — | — |
| `displayName` | text | 显示名（可留空）；留空则显示目标画布的名字并跟着改名；填了就以这里为准；占位：例如：战斗数值主表 | — | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'canvasRef' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
