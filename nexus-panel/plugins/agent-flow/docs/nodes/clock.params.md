# clock — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/clock.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：工具
- **node.type**：`clock`
- **源文件**：`nodes/defs/clock.ts`
- **产出**：text（文本）　**接受**：none
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

格式化后的当前时间

## 注意

- 它**不需要输入**（`接受 = none`），通常作为链的起点。

## 面板上的提示

> 占位符：YYYY 年 · MM 月 · DD 日 · HH 时 · mm 分 · ss 秒 · SSS 毫秒。其它字符原样保留。

共 1 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `format` | text | 格式；占位：YYYY-MM-DD HH:mm:ss | — | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'clock' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
