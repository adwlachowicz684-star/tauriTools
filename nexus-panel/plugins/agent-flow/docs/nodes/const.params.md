# const — 参数与使用方式

> 自动生成，不要手改。

[← 上一层：控件说明](const.md) ｜ [← 回到索引](../README.md)

共 1 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `value` | textarea | 值；名字叫常量，但支持模板，所以实际是"拼一段固定文本"；占位：原样输出给下游；支持 {{模板变量}} | — | — |

## 怎么用它

1. 从侧栏「工具」分组拖到画布
2. 在属性面板填参数（面板由 `nodes/defs/const.ts` 的 fields 自动渲染）
3. 用连线接到上下游；引用上游输出写 `{{上游id.output}}`

产出是文本，可以：
- 直接给下游用（`{{const节点id.output}}`）
- 接「condition」节点做判断

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'const' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
