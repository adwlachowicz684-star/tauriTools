# const — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/const.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：工具
- **node.type**：`const`
- **源文件**：`nodes/defs/const.ts`
- **产出**：text（文本）　**接受**：none
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

常量值（支持模板）

## 注意

- 它**不需要输入**（`接受 = none`），通常作为链的起点。

共 1 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `value` | textarea | 值；名字叫常量，但支持模板，所以实际是"拼一段固定文本"；占位：原样输出给下游；支持 {{模板变量}} | — | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'const' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
