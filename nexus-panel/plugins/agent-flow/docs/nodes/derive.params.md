# derive — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/derive.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：表格
- **node.type**：`derive`
- **源文件**：`nodes/defs/derive.ts`
- **产出**：table（table）　**接受**：table
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

加了新列的表格

## 注意

- 它只接受 table，其余类型接上去会被告警。

## 面板上的提示

> 可用函数：round / floor / ceil / abs / sqrt / min / max / pow / clamp(v,下限,上限) / rand。四则 + - * / % 与 ^（乘方）、括号都支持。列名写错会明确报错，不会静默算成 0。

共 3 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `newCol` | text | 新列名；占位：如：伤害 | — | — |
| `expr` | textarea | 公式；直接写列名，不用加括号；占位：攻击力 * (1 + 暴击率) - 防御力 * 0.5 | — | — |
| `replace` | switch | 允许覆盖同名列；不勾时遇到同名列会报错，避免把原始数据冲掉 | — | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'derive' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
