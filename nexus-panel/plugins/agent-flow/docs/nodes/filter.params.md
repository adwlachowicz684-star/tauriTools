# filter — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/filter.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：表格
- **node.type**：`filter`
- **源文件**：`nodes/defs/filter.ts`
- **产出**：table（table）　**接受**：table
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

筛选后的表格

## 注意

- 它只接受 table，其余类型接上去会被告警。

## 面板上的提示

> 支持 > >= < <= 这种写法 —— 它们会被当成比较，成立为 1、不成立为 0。

共 1 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `cond` | textarea | 保留条件；也可以用算术：暴击伤害 - 1000（结果大于 0 的行保留）；占位：等级 >= 50 | — | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'filter' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
