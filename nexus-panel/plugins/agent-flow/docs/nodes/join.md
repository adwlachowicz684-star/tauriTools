# join — 等所有输入都到齐了才放行下游

> 自动生成，不要手改。源文件：`nodes/defs/join.tsx`

- **分类**：控制器
- **node.type**：`join`
- **产出**：text（文本）
- **接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

所有到齐输入按顺序拼接（宽松模式忽略未走到的分支）

## 参数概览

共 2 项。完整表格与用法见 → [join.params.md](join.params.md)

- `mode`（严格程度）
- `joinBy`（合并分隔符）

---

[← 回到索引](../README.md)
