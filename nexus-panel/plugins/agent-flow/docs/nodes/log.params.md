# log — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/log.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：工具
- **node.type**：`log`
- **源文件**：`nodes/defs/log.ts`
- **产出**：any（透传上游）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

原样透传上游 —— 插在链中间不破坏数据

## 面板上的提示

> 输出原样透传给下游 —— 这个节点不改变数据流，纯粹是给人看的。

共 2 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `level` | select | 级别；ℹ 常规记录 | `info` / `warn` / `error` | — |
| `text` | textarea | 要记的内容；占位：支持 {{上游.output}}；留空则记上游传过来的内容 | — | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'log' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
