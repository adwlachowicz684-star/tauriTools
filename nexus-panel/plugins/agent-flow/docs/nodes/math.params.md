# math — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/math.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：运算
- **node.type**：`math`
- **源文件**：`nodes/defs/math.ts`
- **产出**：text（文本）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

运算结果（数字文本）

## 面板上的提示

> 上游给的是文本，会自动转成数字；转不了的按 0 处理。除数为 0 会报错。

共 3 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `op` | select | 运算 | `add` / `sub` / `mul` / `div` / `mod` / `min` / `max` / `round` / `floor` / `ceil` / `abs` | — |
| `a` | text | 第一个数；占位：支持 {{上游.output}} | — | — |
| `b` | text | 第二个数；占位：支持 {{上游.output}} | — | `d → !['round'` |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'math' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
