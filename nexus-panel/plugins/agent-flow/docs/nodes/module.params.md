# module — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/module.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：工具
- **node.type**：`module`
- **源文件**：`nodes/defs/module.ts`
- **产出**：any（透传上游）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

模块内部最后一个节点的输出

## 面板上的提示

> 模块的内部结构在右侧「编辑内部」里改。改模块库会让所有实例跟着变；改这个实例会让它脱钩成独立副本。

这个控件没有可调参数（结构由画布上的连线决定）。

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'module' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
