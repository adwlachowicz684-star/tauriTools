# random — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/random.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：运算
- **node.type**：`random`
- **源文件**：`nodes/defs/random.ts`
- **产出**：text（文本）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

随机结果

## 面板上的提示

> 每次运行都重新随机 —— 想固定结果就改用常量节点。

共 3 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `op` | select | 随机什么 | `int` / `float` / `pick` / `shuffle` / `bool` | — |
| `a` | text | 最小值 / 选项列表；占位：整数时填最小值；选一个时填 "a,b,c" | — | `d → String(d.op) !== 'bool'` |
| `b` | text | 最大值 | — | `d → ['int'` |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'random' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
