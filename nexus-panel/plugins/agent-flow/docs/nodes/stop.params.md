# stop — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/stop.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：控制器
- **node.type**：`stop`
- **源文件**：`nodes/defs/stop.ts`
- **产出**：any（透传上游）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

透传上游（只是让流程停下）

## 面板上的提示

> 停止不算失败 —— 日志里不会标红。上游数据原样透传给下游。

共 1 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `mode` | select | 停到哪；所有分支都停下 | `all` / `branch` | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'stop' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
