# tableRead — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/tableRead.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：表格
- **node.type**：`tableRead`
- **源文件**：`nodes/defs/tableRead.ts`
- **产出**：table（table）　**接受**：none
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

表格内容（CSV 文本）

## 注意

- 它**不需要输入**（`接受 = none`），通常作为链的起点。

## 面板上的提示

> 第一行必须是表头（列名）。列名会直接用作公式里的变量名，建议不要带空格。

共 2 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `path` | text | 文件路径；CSV / TSV。Excel 里「另存为 → CSV」即可得到；占位：如：/Users/me/数值表.csv | — | — |
| `delim` | select | 分隔符；按第一行里出现最多的那个算 | `,` / `\t` / `;` / `|` | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'tableRead' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
