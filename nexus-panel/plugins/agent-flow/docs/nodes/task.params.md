# task — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/task.tsx` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：任务
- **node.type**：`task`
- **源文件**：`nodes/defs/task.tsx`
- **产出**：text（文本）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

CLI 的执行输出

共 5 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `cli` | select | 使用 CLI | — | — |
| `prompt` | custom | 由手写面板渲染（通常带上游变量插入按钮） | — | — |
| `model` | text | 模型（可选）；部分 CLI 支持指定模型，留空则用它的默认值；占位：留空用 CLI 默认 | — | — |
| `workdir` | text | 工作目录；占位：留空用当前目录 | — | — |
| `yolo` | switch | 省去每次确认，但 CLI 会直接改文件；占位：自动批准工具调用（-y） | — | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'task' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
