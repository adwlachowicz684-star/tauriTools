# text — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/text.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：运算
- **node.type**：`text`
- **源文件**：`nodes/defs/text.ts`
- **产出**：text（文本）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

运算后的文本

## 面板上的提示

> 取子串与取第几段的位置从 1 开始数（第 1 个字符），与直觉一致。

共 4 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `op` | select | 运算 | `concat` / `length` / `upper` / `lower` / `trim` / `replace` / `substr` / `split` / `join` / `repeat` | — |
| `a` | textarea | 文本；占位：支持 {{上游.output}} | — | — |
| `b` | text | 第二个值；占位：拼接的内容 / 要替换掉的文本 / 起始位置 | — | `d → !['length'` |
| `c` | text | 第三个值；占位：替换成 / 结束位置 / 第几段 | — | `d → ['replace'` |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'text' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
