# throttle — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/throttle.tsx` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：控制器
- **node.type**：`throttle`
- **源文件**：`nodes/defs/throttle.tsx`
- **产出**：any（透传上游）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

透传上游；按间隔与次数限制放行节奏

## 面板上的提示

> 间隔是跨运行累计的 —— 第二次运行也会被限制。刷新页面会重置。

共 2 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `minIntervalMs` | number | 最小间隔；两次放行之间至少隔多久（毫秒）。留 0 表示不限 | — | — |
| `maxPerRun` | number | 本次最多放行；超出则失败并阻断下游。留空或 0 表示不限次 | — | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'throttle' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
