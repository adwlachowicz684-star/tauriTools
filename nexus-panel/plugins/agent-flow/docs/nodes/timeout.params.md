# timeout — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/timeout.tsx` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：控制器
- **node.type**：`timeout`
- **源文件**：`nodes/defs/timeout.tsx`
- **产出**：any（透传上游）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

透传上游；整条流程超预算时中断

共 2 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `budgetMs` | number | 预算；毫秒。从**整条流程开始**算起，不是本节点自己的耗时 | — | — |
| `onExceed` | select | 超预算时；失败并阻断下游 | `fail` / `pass` | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'timeout' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
