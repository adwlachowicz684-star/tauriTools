# var — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/var.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：运算
- **node.type**：`var`
- **源文件**：`nodes/defs/var.ts`
- **产出**：any（透传上游）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

写模式：写入的值；读模式：变量的值

## 面板上的提示

> 读取时用 {{var.变量名}} 也能取到。变量没赋值过就读不出来，会明确报错而不是给空值。

共 3 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `mode` | select | 操作；把值存进这个变量 | `set` / `get` | — |
| `name` | text | 变量名；占位：如：总数、上次时间 | — | — |
| `value` | textarea | 值；支持模板；占位：留空则用上游输出 | — | `d → d.mode !== 'get'` |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'var' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
