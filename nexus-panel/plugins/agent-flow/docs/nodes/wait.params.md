# wait — 参数与使用方式

> 自动生成，**不要手改**。这是 `nodes/defs/wait.ts` 的**派生视图**：
> 具体值以源文件为准，这里只做汇总。

[← 回到索引](../README.md)

- **分类**：工具
- **node.type**：`wait`
- **源文件**：`nodes/defs/wait.ts`
- **产出**：any（透传上游）　**接受**：any
- **需要的外部能力**：无（纯本地，浏览器模式也能跑）

## 它做什么

透传上游（只是延时，状态写进运行日志）

## 面板上的提示

> 单次最多 10 分钟，超了会按上限执行并提示。

共 1 项：

| 参数 | 类型 | 说明 | 取值 | 显示条件 |
|---|---|---|---|---|
| `ms` | number | 等待时长（毫秒）；1000 = 1 秒。支持模板，如 {{上游.output}} | — | — |

## 建节点的正确方式

用 `def.create()`（即 `makeXxxNode`）建节点，它会填好默认值。
手工拼 `{ kind: 'wait' }` 会缺默认字段 ——
本控件没有隐藏字段，但用 create() 仍是推荐做法。
