# 脑图功能剩余项清单

> 生成日期：2026-09-16 · **最后更新：2026-09-19**
> 依据：`WPF脑图功能缺失清单.md`（100 项）逐条核对后的收尾状态
> 已完成 16 批（清单 12 批 + 新增能力 4 批），测试 **1318 项全通过**

---

## 一、总览

| 分类 | 数量 | 说明 |
|---|---|---|
| **已完成** | 94 | 含 15 项经核实为误判（原就实现 / 内核已提供 / WPF 无此功能） |
| **建议不做** | 4 | Web 语境不成立，硬做只会是个空壳 |
| **可做可不做** | 2 | 低优先级打磨项（**已于第 12 批完成**） |

**结论：清单里「确认该做」的项已全部完成，含最后 2 项打磨项。**

> 清单完成后，又按你的要求追加了 **4 批「清单外的新增能力」**（第六节）：
> 多格式互转 → 拖放附加 → 多附件 → 审查修复。这些不在原 100 项里，
> 是 Web 版相对 WPF 的**增强**，故单列一节记录。

---

## 二、建议不做（4 项）

| 编号 | 功能 | 为什么不做 |
|---|---|---|
| **A68** | 安装/卸载脑图组件 | WPF 的「脑图组件」是**可插拔模块**，装了才有脑图面板、没装显示占位。Web 版是单页应用里的一个面板，**没有「安装」这个概念**。硬做只能是个假开关 |
| **A69** | 组件未启用占位 | 同上，依附于 A68 存在。若要保留一点价值，可降级为**「面板显隐开关」** |
| **A10** | ico 多尺寸转换 | 已用 **SVG 图标**替代 `.ico` —— SVG 天然无损缩放，不需要多尺寸。图标库那 23 个内置图标都是手写的 SVG path |
| **B19** | ico 最大帧转 PNG | 同上，转换逻辑不适用 |

---

## 三、可做可不做（2 项，✅ 已于第 12 批完成）

| 编号 | 功能 | 修复前 | 修复后 |
|---|---|---|---|
| **A29** | 搜索失败状态区分 | 三种情况统一显示「无匹配」 | 三态分流：请输入关键字 / 编辑器未就绪 / 搜索失败 / 无匹配 |
| **B22** | XMind 跨机迁移提示 | 导入无资源的 JSON 无任何提示 | 导入后立刻提示「N 个附件本机找不到，请改用 .xmind」 |

### A29 的三态为什么必须分开

原实现三种情况都显示「无匹配」——**尤其「编辑器未就绪」这一种最误导**：
用户刚打开画布还在加载时搜索，看到「无匹配」会以为自己的脑图里真没有那个词。

根因在 `EditorBridge.search()` 的兜底：
`_safe(...) || { total: 0, index: 0, text: '' }` 把「未就绪」和「搜了没结果」
**压成了同一个结果**。改成返回 `ok:false + reason` 后三态才分得开。

另外零结果是**正常结果不该报警**，前三种才是提醒——视觉上用 `.warn` 区分。

### B22 为什么必须在导入时立刻说

本插件的 `.json` 只存引用（`{a:'<assetId>'}`），附件本体在 IndexedDB。
换台机器导入后，**引用看着正常、附件图标也在，但字节没跟过来** ——
不提前提示的话，用户要一直点到那个节点才发现打不开，那时会以为是文件坏了。

提示要说清两件事：**为什么**（JSON 只带引用不带本体）+ **怎么办**（改用 .xmind，它会打包附件）。

只针对 .json；.xmind 会把附件打包进去，不存在这个问题，故不在那条分支调用。



| 编号 | 功能 | 现状 | 代价 |
|---|---|---|---|
| **A29** | 搜索失败状态区分 | 三种情况（空关键字 / 未就绪 / 零结果）**统一显示「无匹配」** | 改起来只需分流文案，约 10 行 |
| **B22** | XMind 跨机迁移提示 | 导入无资源的 `.xmind` 时没有提示「需重新打包」 | 需在导入分支加一句判断 |

> 这两项都不影响功能正确性，属于「有更好、没有也不出错」。

---

## 四、经核实为误判的 15 项（记录以免重犯）

这是本轮最大的收获 —— **如果没核实，会去「补」一堆根本不存在的东西。**

### 4.1 原就实现，清单判「完全缺失」（9 项）

A12 清除图片 · A25 选择全选/反选/兄弟/同层/路径/子树 · A26/A27 展开层级 ·
A28 搜索定位计数 · A45 最近文件恢复列表 · A46 从备份恢复覆盖当前 ·
A63 自定义主题编辑覆盖 · B13 外框标签编辑

### 4.2 内核本来就提供（2 项）

| 编号 | 功能 | 内核实现 |
|---|---|---|
| B2 | 剪切/复制/粘贴 | `ClipboardModule` 提供 `copy`/`cut`/`paste`，已注册 Ctrl+C/X/V |
| B3 | 多选拖拽移动 | `DragTree` 拖拽源取 `getSelectedAncestors()`，天然支持多选 |

### 4.3 WPF 原版根本没有（4 项）

| 编号 | 功能 | 核实结果 |
|---|---|---|
| A34–A37 / A41 / B29 | 打印与 PDF | 全仓搜 `Print` **零命中**（唯一 `PrintWindow` 是截窗口的 Win32 API） |
| B14 | 概要节点 | 全仓 `summary` 命中**全是 C# XML 文档注释** `/// <summary>`，脑图源码零命中 |
| B15 | 关联线 | `relation` 在脑图代码里零命中 |

### 4.4 Web 端天然成立，无需实现（1 项）

| 编号 | 功能 | 核实结果 |
|---|---|---|
| **B7** | 样式面板防回写 | WPF 需要 `_syncingStyle` 抑制 `SelectionChanged` 回发；Web 端**程序化赋值不触发 change 事件**（已实测确认），天然没有这个问题 |

### 4.5 误判的成因（已固化成检查方法）

1. **只搜字符串字面量会漏判。** min.js 里搜 `"copy"`/`"cut"`/`"paste"` 是**零命中**的 ——
   命令写作 `commands:{copy:i,cut:j,paste:k}`，是**标识符**不是字符串。
2. **没定义 `queryState` ≠ 不生效。** 基类返回 `STATE_NORMAL`(0，不是 -1)，
   快捷键判定 `-1 !== state` 是**能**触发的。
3. **清单里凡标注「未定位到确切行号」的，都必须先核实再动手。**

---

## 五、第 13 批新增：多格式互转（非清单项，属新增能力）

新增 `formats.js`，支持与市面主流工具交换文件：
**FreeMind `.mm` / OPML `.opml` / Mermaid `.mmd` / PlantUML `.puml`**（均支持导入与导出）。

要点：
- 这些都是**单画布**格式 —— 导出只写当前画布并明确提示；导入用文件名当标题
- 只交换「文字 + 层级 + 折叠状态」，图标/优先级/附件一概不写（塞进去只会变乱码）
- 格式识别**内容优先于扩展名**（用户常把 .opml 存成 .xml）

详见 README「第 13 批」。

## 五·二、第 14 批：拖放附加（图片 / 视频 / 文件）

把文件拖到节点上即附加，省去「选中 → 侧栏翻页 → 点按钮」。

| 拖到 | 行为 |
|---|---|
| 节点上 | 附加到该节点 |
| 空白处 | 忽略、不建节点，但**会提示**（不提示用户只会觉得"拖了没反应"） |

不建节点是刻意的：拖放容错差，误建的空白节点清理起来很烦。

**三个实测约束（都不是风格选择）**
1. `dragover` 必须 `preventDefault()` —— 否则浏览器根本不派发 `drop`（默认行为是"打开文件"，会直接跳转走）
2. 原生拖拽期间**不派发 `mousemove`** —— 拿不到"当前悬停节点"，只能从 `drop` 的 target 反查 DOM
3. 映射必须 `WeakMap` —— 画布重渲极其频繁，普通 `Map` 会稳定泄漏

## 五·三、第 15 批：多附件（一个节点可挂多个）

你要求：一次拖 3 个文件 → **全部挂在同一个节点上**，向下依次排开（不再建子节点）。

存储随之改了：`file` / `video` 从单值变成 **JSON 数组串**，新增 `images` 存图片数组。

| 类型 | 画布呈现 |
|---|---|
| 图片 | 横幅，多张可左右切换，带「i/n」计数 |
| 视频 | 一张卡片 + 右上角**数字角标**（显示总数） |
| 文件 | 每行一个（图标 + 文件名），行距 17px |

**三个必须守住的边界**
- **写一定是数组，读两者都认** —— 老数据（C# 迁移 / 旧 Web 版）是单对象串或纯路径串，读取时包成单元素数组
- **单张图走 `image`（内核框内），≥2 张才走 `images`（横幅）** —— 两个字段同时有值会让同节点画出两张图
- **角标数字用深色** —— 圆底跟随节点色（可能很浅），数字也跟随的话在浅色主题上就看不见

## 五·四、第 16 批：从功能到显示过一遍（审查）

抓到三处真问题，都不是代码风格问题，是会让用户明确困惑的：

| # | 问题 | 后果 |
|---|---|---|
| 1 | 点画布上的图片走 `openAttachment` | `decodeRef(dataURL)` 会兜底成 `legacyPath`（`a` 为 null）→ 报「旧版本地路径，无法打开」 |
| 2 | XMind 导入的写回时机 | `out[idx]` 在 `saveAsset` resolve 前还是**占位值**（包内路径），提前写回会把引用固化成 `resources/kma_0_…` → **导出再导入后所有附件都打不开** |
| 3 | 侧栏选中索引写在 `pageFile` 内 | `refresh()` 重建 DOM 时每次归 0 → 点第 3 个文件看详情，刷新完又跳回第 1 个，表现为"点了没反应" |

第 2 条是往返测试实锤的：期望 `一.pdf,二.pdf,三.pdf`，
实际 `kma_0_一.pdf,kma_1_二.pdf,kma_2_三.pdf` 且所有 `a` 都是 null。

## 六、如果要继续，建议做这两件事

### 5.1 先把 Rust 编译验证掉（**最重要**）

前几批累积了 3 个 Rust 命令，**全部未经 `cargo build`**（沙盒无 cargo/rustc）：

| 命令 | 用途 | 风险点 |
|---|---|---|
| `mm_print` | 原生打印 | wry 只在 macOS 实现，其它平台是 no-op |
| `mm_svg_to_pdf` | SVG → PDF（矢量） | **新增了 `svg2pdf` 依赖**，需拉 ~8–13MB |
| `mm_open_devtools` | 开发者工具 | release 构建下 `open_devtools` 可能不存在 |

尤其是 `mm_svg_to_pdf` —— 它是当前的**默认 PDF 通道**，编译不过等于导出功能挂掉。
且已知限制：**Linux 可能缺中文字体**（字体族写死 `Arial, "Microsoft YaHei","Heiti SC"`）。

### 5.2 一轮真实环境实测

以下在 jsdom 里**测不到**，只能你在 Tauri 里确认：

- 非 100% 缩放下双击编辑节点（字号/位置是否对齐）
- 工具栏按钮点完按 Tab 能否插入子节点
- 页签拖拽手感（死区 / 跟随 / 边缘滚动速度）
- 打印预览中深色主题的背景色有没有印出来
- A71 诊断窗口能否真的收到 iframe 的报错
- **拖 3 个文件到同一节点** → 应看到 3 行往下排
- **拖 2 张图片** → 横幅 + 「1/2」+ 左右箭头能否切换
- **拖 2 个视频** → 角标是否显示 **2**
- **导出 .xmind 再导入** → 附件是否都还在且能打开（第 16 批第 2 条的回归验证）
- **点画布上的图片** → 应直接弹出预览，而不是报"旧版本地路径"
- **侧栏点第 3 个文件** → 详情区是否跟着切换且有高亮

---

## 七、剩余的「大工程」选项

如果你还想继续扩展功能，唯一值得立项的是**从零开发**这两项
（WPF 原版没有，属新增能力，非移植）：

| 功能 | 工作量 | 说明 |
|---|---|---|
| **概要节点** | 大 | 内核零命中，需自建渲染 + 布局 + 导出协议 |
| **关联线** | 大 | 同上，还要处理跨分支连接与持久化 |

单项工作量约等于前几批总和，**建议单独立项讨论**，不建议顺手做。
