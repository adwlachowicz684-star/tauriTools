# 脑图功能剩余项清单

> 生成日期：2026-09-16
> 依据：`WPF脑图功能缺失清单.md`（100 项）逐条核对后的收尾状态
> 已完成 11 批，测试 1017 项全通过

---

## 一、总览

| 分类 | 数量 | 说明 |
|---|---|---|
| **已完成** | 94 | 含 15 项经核实为误判（原就实现 / 内核已提供 / WPF 无此功能） |
| **建议不做** | 4 | Web 语境不成立，硬做只会是个空壳 |
| **可做可不做** | 2 | 低优先级打磨项 |

**结论：清单里「确认该做」的项已全部完成。**

---

## 二、建议不做（4 项）

| 编号 | 功能 | 为什么不做 |
|---|---|---|
| **A68** | 安装/卸载脑图组件 | WPF 的「脑图组件」是**可插拔模块**，装了才有脑图面板、没装显示占位。Web 版是单页应用里的一个面板，**没有「安装」这个概念**。硬做只能是个假开关 |
| **A69** | 组件未启用占位 | 同上，依附于 A68 存在。若要保留一点价值，可降级为**「面板显隐开关」** |
| **A10** | ico 多尺寸转换 | 已用 **SVG 图标**替代 `.ico` —— SVG 天然无损缩放，不需要多尺寸。图标库那 23 个内置图标都是手写的 SVG path |
| **B19** | ico 最大帧转 PNG | 同上，转换逻辑不适用 |

---

## 三、可做可不做（2 项）

| 编号 | 功能 | 现状 | 代价 |
|---|---|---|---|
| **A29** | 搜索失败状态区分 | 三种情况（空关键字 / 未就绪 / 零结果）**统一显示「无匹配」** | 改起来只需分流文案，约 10 行 |
| **B22** | XMind 跨机迁移提示 | 导入无资源的 `.xmind` 时没有提示「需重新打包」 | 需在导入分支加一句判断 |

> 这两项都不影响功能正确性，属于「有更好、没有也不出错」。

---

## 四、经核实为误判的 15 项（记录以免重犯）

这是本轮最大的收获 —— **如果没核实，会去「补」一堆根本不存在的东西。**

### 4.1 原就实现，清单判「完全缺失」（9 项）

A12 清除图片 · A25 选择全选/反选/兄弟/同层/路径/子树 · A26/A27 展开层级 ·
A28 搜索定位计数 · A45 最近文件恢复列表 · A46 从备份恢复覆盖当前 ·
A63 自定义主题编辑覆盖 · B13 外框标签编辑

### 4.2 内核本来就提供（2 项）

| 编号 | 功能 | 内核实现 |
|---|---|---|
| B2 | 剪切/复制/粘贴 | `ClipboardModule` 提供 `copy`/`cut`/`paste`，已注册 Ctrl+C/X/V |
| B3 | 多选拖拽移动 | `DragTree` 拖拽源取 `getSelectedAncestors()`，天然支持多选 |

### 4.3 WPF 原版根本没有（4 项）

| 编号 | 功能 | 核实结果 |
|---|---|---|
| A34–A37 / A41 / B29 | 打印与 PDF | 全仓搜 `Print` **零命中**（唯一 `PrintWindow` 是截窗口的 Win32 API） |
| B14 | 概要节点 | 全仓 `summary` 命中**全是 C# XML 文档注释** `/// <summary>`，脑图源码零命中 |
| B15 | 关联线 | `relation` 在脑图代码里零命中 |

### 4.4 Web 端天然成立，无需实现（1 项）

| 编号 | 功能 | 核实结果 |
|---|---|---|
| **B7** | 样式面板防回写 | WPF 需要 `_syncingStyle` 抑制 `SelectionChanged` 回发；Web 端**程序化赋值不触发 change 事件**（已实测确认），天然没有这个问题 |

### 4.5 误判的成因（已固化成检查方法）

1. **只搜字符串字面量会漏判。** min.js 里搜 `"copy"`/`"cut"`/`"paste"` 是**零命中**的 ——
   命令写作 `commands:{copy:i,cut:j,paste:k}`，是**标识符**不是字符串。
2. **没定义 `queryState` ≠ 不生效。** 基类返回 `STATE_NORMAL`(0，不是 -1)，
   快捷键判定 `-1 !== state` 是**能**触发的。
3. **清单里凡标注「未定位到确切行号」的，都必须先核实再动手。**

---

## 五、如果要继续，建议做这两件事

### 5.1 先把 Rust 编译验证掉（**最重要**）

前几批累积了 3 个 Rust 命令，**全部未经 `cargo build`**（沙盒无 cargo/rustc）：

| 命令 | 用途 | 风险点 |
|---|---|---|
| `mm_print` | 原生打印 | wry 只在 macOS 实现，其它平台是 no-op |
| `mm_svg_to_pdf` | SVG → PDF（矢量） | **新增了 `svg2pdf` 依赖**，需拉 ~8–13MB |
| `mm_open_devtools` | 开发者工具 | release 构建下 `open_devtools` 可能不存在 |

尤其是 `mm_svg_to_pdf` —— 它是当前的**默认 PDF 通道**，编译不过等于导出功能挂掉。
且已知限制：**Linux 可能缺中文字体**（字体族写死 `Arial, "Microsoft YaHei","Heiti SC"`）。

### 5.2 一轮真实环境实测

以下在 jsdom 里**测不到**，只能你在 Tauri 里确认：

- 非 100% 缩放下双击编辑节点（字号/位置是否对齐）
- 工具栏按钮点完按 Tab 能否插入子节点
- 页签拖拽手感（死区 / 跟随 / 边缘滚动速度）
- 打印预览中深色主题的背景色有没有印出来
- A71 诊断窗口能否真的收到 iframe 的报错

---

## 六、剩余的「大工程」选项

如果你还想继续扩展功能，唯一值得立项的是**从零开发**这两项
（WPF 原版没有，属新增能力，非移植）：

| 功能 | 工作量 | 说明 |
|---|---|---|
| **概要节点** | 大 | 内核零命中，需自建渲染 + 布局 + 导出协议 |
| **关联线** | 大 | 同上，还要处理跨分支连接与持久化 |

单项工作量约等于前几批总和，**建议单独立项讨论**，不建议顺手做。
