# 脑图功能开发计划（核实后重排）

> 生成日期：2026-09-16
> 依据：`WPF脑图功能缺失清单.md`（100 项）+ 逐条核对 `kityminder.core.min.js` 与现有 Web 实现
> 已完成：A1–A14、A53–A57、A60、A61、B1（4 批）+ **P0：A62/A64/A67/A47/A42**（第 5 批）
> + **P1：附件与视频 A15–A24/B20/B23/B24**（第 6 批，10 项合并为一次改造）
> + **P2：导入导出 A30/A31/A38/A39**（第 7 批；其中 A32/A33/A40 **经核实早已实现**，未重复做）
> + **P3：打印/PDF A34–A37/A41/B29**（第 8 批；**经核实 WPF 原版无此功能**，改按 Web 新增实现）
> + **P3b：PDF 默认改走 svg2pdf 矢量通道**（第 9 批；打印对话框降级为托底，设置可改默认）
> + **审查修复 + A65/A66 边界 + A5/A18/A70/A71**（第 10 批，见下）
> + **B27 导出同步修复 + B4/B5 档位补项 + B6 颜色名**（第 11 批，见下）

---

## 一、先纠偏：清单有 15 项误判

开工核对后发现，清单里有相当一部分标注「完全缺失」的项**其实已经实现**，
或**内核本来就提供**。这些若不先剔除，会白白重复造轮子（B2 就是教训：
中途写过一套 `copynode/cutnode/pastenode`，结果 `cutnode:'ctrl+x'` 会
**覆盖**内核已注册的 Ctrl+X，不得不撤回）。

### 1.1 已实现（清单误判，无需再做）

| 编号 | 功能 | 清单判 | 实际状态 |
|---|---|---|---|
| A12 | 清除图片 | 完全缺失 | ✅ 标签页「清除图标」按钮（`setImage(null)`） |
| A13/A14 | 优先级/进度 0 清除 | 完全缺失 | ✅ 徽章清除格（第 3 批） |
| A25 | 选择 全选/反选/兄弟/同层/路径/子树 | 完全缺失 | ✅ 已实现 + **6 个工具栏按钮** |
| A26 | 展开到层级 | 完全缺失 | ✅ `expandSelectedToLevel()` |
| A27 | 展开选中子树 N 层 | 完全缺失 | ✅ Alt+1~5 |
| A28 | 逐一定位搜索计数 | 完全缺失 | ✅ 搜索框 + 定位 + `index/total` |
| A45 | 最近文件恢复列表 | 完全缺失 | ✅ 设置 → 历史快照 |
| A46 | 从备份恢复覆盖当前 | 完全缺失 | ✅ `restoreBackup()` |
| A63 | 自定义主题编辑覆盖 | 完全缺失 | ✅ `filter(x=>x.id!==id).push(editing)` |
| B13 | 外框标签编辑 | 完全缺失 | ✅ 工具栏「外框」+ `boundary` 命令 |

### 1.2 内核已提供（无需实现）

| 编号 | 功能 | 内核实现 |
|---|---|---|
| B2 | 剪切/复制/粘贴节点 | `ClipboardModule` → `copy`/`cut`/`paste`，已注册 Ctrl+C/X/V |
| B3 | 多选拖拽移动 | `DragTree`，拖拽源取 `getSelectedAncestors()`，天然支持多选 |

### 1.3 误判的成因（记下来，避免重犯）

排查 B2 时踩了两个坑，值得固化成检查方法：

1. **只搜字符串字面量会漏判。** 在 min.js 里搜 `"copy"` / `"cut"` / `"paste"`
   是**零命中**的 —— 命令写作 `commands:{copy:i,cut:j,paste:k}`，是**标识符**。
   → 正确做法：搜 `createClass("XxxCommand"` 和 `commands:{...}` 结构。
2. **没定义 `queryState` ≠ 不生效。** `copy`/`cut` 确实没自定义 `queryState`，
   但基类返回 `STATE_NORMAL`(0，不是 -1)，快捷键回调判定 `-1 !== state` → **能**触发。

**结论：清单里凡是标注「未定位到确切行号」的项，都必须先核实再动手。**
本次核实后，剩余 102 条里这一类还有约 20 条。

---

## 二、剩余任务全景（约 60 项真缺失）

按**域**重新分组。原来的 A/B 编号保留以便追溯，但**执行时按域整批做**，
因为同域内各项共享数据结构与 UI 容器，拆开做会反复重构。

### 域 1：主题管理（5 项）— 建议优先

| 编号 | 功能 | 现状 |
|---|---|---|
| A62 | 自定义主题删除后回退 | ❌ 删掉正在用的主题后无回退，画布可能停在已注销的主题上 |
| A64 | 以当前主题作新建种子 | ❌ 新建永远是空白 `blankTheme()` |
| A67 | 自定义主题注册顺序 | ❌ 切换画布/重载后注册顺序不定 |
| A65/A66 | 主题 JSON 导入/导出 | ⚠️ 有 UI，需核对边界（重名、非法 JSON） |

> **A62 是真 bug 不只是缺失**：删除正在使用的主题，若不同步切回内置主题，
> 画布会引用一个已注销的主题 id。

### 域 2：附件与视频（10 项）

| 编号 | 功能 |
|---|---|
| A15 | 附件用默认程序打开 |
| A16 | 附件路径失效状态 |
| A17 | 附件目录 / 创建时间 |
| A19 | 视频自动播放策略 |
| A20 | 视频进度条拖拽 |
| A21 | 视频播放/暂停状态 |
| A23 | 附件移除先询问替换 |
| A24 | 节点点击附件打开请求 |
| B20 | 附件卡片播放状态 |
| B23/B24 | 文件类型 / 修改时间 / 大小格式化 |

> 这 10 项**高度重叠** —— 都在「附件信息卡」这一个 UI 容器里。
> **建议合并成一次改造**：重做附件卡片的数据结构（补 type/mtime/ctime），
> 视频播放器换成带状态的控件。分 10 次做会把同一个组件重写 10 遍。

### 域 3：备份与恢复（2 项）

| 编号 | 功能 |
|---|---|
| A42 | 自动备份目录配置与迁移 |
| A47 | 自动备份暂停/恢复 |

（A45/A46 已实现，见 1.1）

### 域 4：导入导出（8 项）

| 编号 | 功能 | 备注 |
|---|---|---|
| A30 | 多画布整体替换导入 | 数据结构层面 |
| A31 | 单/多画布包自动识别 | 数据结构层面 |
| A32 | 多画布 Markdown 导出 | |
| A33 | 整本 XMind 无损快照 | 现为单画布 |
| A38 | 高分辨率整图 PNG | 部分实现 |
| A39 | 导出格式右键菜单 | 部分实现 |
| A40 | 导出命名时间戳 | 部分实现 |

### 域 5：打印与 PDF（8 项）— 高风险，建议后置

A34 打印 / A35 打印预览 / A36 系统打印对话框 / A37 PDF 导出 / A41 缩略图预览窗口 / B29

> ⚠️ **WPF 直接调 `PrintDialog` / `Process.Start`，Web 版没有对应能力。**
> Tauri 里要走 Rust 侧命令或 `opener` 插件，且 PDF 需要额外依赖
> （如 `printpdf` 或调系统命令）。**不能直接照搬，必须重新设计交互。**

### 域 6：图标库补余（2 项）

A5 剪贴板位图导入 / A18 预设图标失效清理

### 域 7：结构能力（2 项）— 工作量大

B14 概要节点 / B15 关联线（关系线）

> 内核**没有**这两个能力（`summary`/`relation` 在 min.js 里零命中）。
> 需要自建渲染 + 布局 + 导出协议，**是真正的从零开发**，单项工作量≈前 4 批总和。

### 域 8：组件宿主与诊断（4 项）

A68 安装/卸载组件 / A69 未启用占位 / A70 开发者工具 / A71 诊断错误捕获

> ⚠️ **A68 在 Web 语境下不成立。** WPF 的「脑图组件」是可插拔模块，
> 装了才有脑图面板、没装显示占位；Web 版是单页应用里的一个面板，
> 没有「安装」这个概念。**建议降级为不做**，或改造成「面板显隐开关」。

---

## 三、分阶段计划

按「**风险 → 价值 → 成本**」排序，而非按编号顺序。

### P0｜数据可靠性（✅ 已完成）

**域 3 备份（2 项）+ 域 1 主题修复（A62、A64、A67）**

理由：A62 是**真 bug**（删主题后画布引用失效），A42/A47 与已完成的
A48/A49 同属「防丢数据」类。这类问题一旦发生**不可逆**，优先于任何交互补齐。

- A42 备份目录配置与迁移
- A47 备份暂停/恢复
- A62 主题删除回退 ← **修 bug**
- A64 新建主题以当前为种子
- A67 主题注册顺序

### P1｜高频交互补齐（✅ 已完成）

**域 2 附件与视频（合并为一次改造，10 项）**

附件是日常高频操作，且这 10 项共享同一个 UI 容器，合并做最划算。

### P2｜导入导出补全（✅ 已完成）

**域 4（7 项）**，先做数据结构层（A30/A31/A32/A33），再做导出细节（A38/A39/A40）。

### P3｜打印与 PDF（✅ 已完成 —— 但**不是移植项**，见下）

**【开工前核实：这 8 项不是移植缺失】** WPF 全仓搜 `Print` 零命中
（唯一 `PrintWindow` 是截窗口的 Win32 API），A34–A37/A41/B29 六项出处
全是「未定位到确切行号」—— 属未经核实的推测。已改判为 **Web 版新增能力**。

**域 5（8 项）**。技术路线：
- 走 Tauri Rust 命令（要写 Rust 代码，需你确认是否接受）
- 还是用浏览器 `window.print()` + 打印样式表（成本低但能力弱）

### P4｜大工程（量力而行）

**域 7 概要/关联线（2 项）**，从零开发，建议单独立项。

### 建议不做 / 降级

| 编号 | 处置 | 理由 |
|---|---|---|
| A68/A69 | 降级 | Web 版无可插拔组件概念，或改造为「面板显隐开关」 |
| B19 | 不做 | 已用 SVG 替代 .ico，ico→PNG 转换不适用 |
| A10 | 不做 | 同上，ico 多尺寸转换不适用 |

---

## 四、依赖关系

```
A42 备份目录 ──┬─→ A47 备份暂停/恢复
               └─→ （A45/A46 已实现，接入即可）

A62 主题删除回退 ─→ A64 新建种子 ─→ A67 注册顺序
（都要动 customThemes 的写入路径，一起改）

域 2 附件改造：数据层（type/mtime/ctime）─→ UI 层（卡片）─→ 视频播放器

域 4：A31 包格式识别 ─→ A30 多画布导入 ─→ A33 整本 XMind 快照
```

---

## 五、执行原则（沿用前 4 批已验证的做法）

1. **先核实再实现。** 凡清单标注「未定位到确切行号」的，先查内核与现有代码。
2. **每批加防回归断言。** 已用这套方法抓到 B2 的重复实现风险。
3. **变异验证。** 关键断言必须验证「改坏了会变红」，否则可能是空断言。
4. **沙盒测不到的要说明。** jsdom 不做布局计算、无 Chromium，
   视觉与真实交互必须你在 Tauri 里确认 —— 前几批已多次证明这一环不可省。

---

### P5｜审查修复 + 收尾（✅ 已完成，第 10 批）

先审一遍已完成批次，抓出两个**真 bug**（都是我自己上一批引入的）：

| # | 问题 | 说明 |
|---|---|---|
| 1 | `exportPdf` 误报成功 | `saveBlob` 有 ok/cancel/fallback/error 四种返回，其余 7 处导出都用 `reportSave` 正确处理，只有这里写成 `if (r !== 'cancel') 就报成功` —— **error 也落进那个分支**，变成「保存失败却提示已导出」 |
| 2 | 主题导入未校验颜色 | A64（新建主题）早就发现 `sub='transparent'` 写进 palette 会让**整个主题失效**并做了回落；但**导入路径完全没有**这层校验 —— 同一个坑在两条路上不对称 |

**教训**：这两个都是「同一件事有两条代码路径、只修了一条」。
后面凡是遇到双路径（新建/导入、矢量/对话框），都要检查两边是否对称。

随后完成剩余小项：

- **A65/A66 主题 JSON 导入/导出边界**：`sanitizePalette()` 校验色值与数值、
  导入重名加序号、被修正的字段要告诉用户、导出提示与其余 7 处对齐
- **A5 剪贴板位图导入**：走 `paste` 事件（不弹授权框），挂 document 且**关闭时解绑**
- **A18 失效图标清理**：`pruneMissing()` 移除资产已丢失的条目
- **A70 开发者工具**：Rust `mm_open_devtools`，**release 明确报错**（给最终用户开控制台没意义）
- **A71 诊断捕获**：iframe 与外壳双向捕获，环形缓冲保留最近 100 条

### P6｜B27 修复 + 样式面板收口（✅ 已完成，第 11 批）

**B27 是真 bug：`exportMarkdown` 漏了 `capture()`。**
它读的是 `workbook.sheets`（内存数据结构），不是编辑器实时状态 ——
少了这一句，用户改完节点立刻导出会拿到**改之前**的内容。

本次**先核实再动手**避免了误修：初步扫描显示 exportMarkdown / exportPng /
exportPdf / printMap 四个都"缺 capture"，但后三个走 `bridge.exportSvg()`
直接从编辑器取当前画面，**本来就是实时的**，给它们加反而是多余的序列化开销。
真需要修的只有 exportMarkdown 一处。

随后完成样式面板收口：
- **B4/B5** 字号下拉动态补项（`withPresetValue`）：节点字号来自导入文件/
  样式刷/旧版本数据时可能不在预设档位里，此时下拉**无任何项选中**，
  显示成空白或第一项 —— 用户会以为"字号是 12"实际是 13，随手一改就把值弄丢了
- **B6** `parseColor` 兼容 CSS 颜色名。刻意**不含** `transparent`：
  透明没有 RGB 值，硬映射成黑色会让"这色能不能用"的判断失真

> **`withPresetValue` 修掉一个实现缺陷**：`Number(null)` 是 0 且
> `Number.isFinite(0)` 为真 —— 不显式挡掉 null/''，
> 「节点没设字号」会往下拉里塞一个 **0** 选项。这是测试抓出来的。

### B14/B15 经核实是误判（第 11 批核实）

| 编号 | 清单判 | 核实结果 |
|---|---|---|
| B14 概要节点 | 完全缺失 | **WPF 原版没有**。全仓 `summary` 命中全是 C# XML 文档注释 `/// <summary>`，脑图相关源码零命中 |
| B15 关联线 | 完全缺失 | **WPF 原版没有**。`relation` 在脑图代码里零命中 |

两条在清单里都标注「未定位到确切行号」，分类本就是「B-优化建议」而非「A-缺失功能」。
这是**第 4 次**抓出同类误判（前三次：P3 打印、B2 剪贴板、B3 多选拖拽）。

### 仍未做

| 编号 | 处置 | 理由 |
|---|---|---|
| B14/B15 | 待立项 | 概要节点/关联线 —— 内核零命中，需自建渲染+布局+导出，单项工作量≈前几批总和 |
| A68/A69 | 建议不做 | Web 版无可插拔「组件」概念 |

### P7｜清单外的「新增能力」四批（✅ 已完成，第 13–16 批）

清单 100 项做完后，按你的要求追加的 **Web 版增强**（非移植项）。
这些 WPF 原版没有，是 Web 版多出来的能力，故不计入清单统计。

| 批次 | 内容 | 关键点 |
|---|---|---|
| **13** | 多格式互转 | FreeMind `.mm` / OPML `.opml` / Mermaid `.mmd` / PlantUML `.puml`，导入导出都支持 |
| **14** | 拖放附加 | 文件拖到节点即附加；`dragover` 必须 preventDefault、拖拽期间无 mousemove、映射用 WeakMap |
| **15** | 多附件 | `file`/`video` 改数组串，新增 `images`；图片横幅 / 视频数字角标 / 文件每行一个 |
| **16** | 审查修复 | 点图误报旧路径、xmind 写回时机、侧栏选中归零（详见下） |

**第 16 批的三处修复，都是「功能对、显示/时机错」** —— 单看代码逻辑分支是通的，
跑起来才暴露：

1. 图片是 dataURL，`decodeRef` 会兜底成 `legacyPath`（实测 `a` 为 null）→ 点画布上的图报"旧版本地路径"
2. `out[idx]` 在 `saveAsset` resolve 前是占位值，提前写回会把引用固化成 `resources/kma_0_…`
   → **导出再导入后所有附件都打不开**
3. 侧栏选中索引写在 `pageFile` 内 → `refresh()` 每次归 0 → "点了没反应"

## 六、下一步

**P0–P7 已全部完成**，测试 1318 项全通过。剩下的只有两类事：

### 6.1 把 Rust 编译验证掉（**最高优先级**）

累积了 3 个 Rust 命令**全部未经 `cargo build`**（沙盒无 cargo/rustc）：

| 命令 | 风险 |
|---|---|
| `mm_svg_to_pdf` | **默认 PDF 通道**，且新拉了 `svg2pdf` 依赖（~8–13MB）。编译不过 = 导出挂掉 |
| `mm_print` | wry 只在 macOS 实现原生打印，其它平台是 no-op |
| `mm_open_devtools` | release 下 `open_devtools` 可能不存在 |

### 6.2 一轮真实环境实测

jsdom 里 `getBoundingClientRect` 全返回 0、video 不解码、无真实打印后端，
以下都**测不到**：

- 非 100% 缩放下双击编辑节点
- 工具栏按钮点完按 Tab 能否插入子节点
- 页签拖拽手感（死区 / 跟随 / 边缘滚动）
- 打印预览中深色背景有没有印出来
- 诊断窗口能否收到 iframe 报错
- 拖 3 个文件 / 2 张图片 / 2 个视频的画布呈现
- **导出 .xmind 再导入**（第 16 批第 2 条的回归验证）
- 点画布图片是否直接预览、侧栏点第 3 个文件详情是否跟随

### 6.3 之后若要继续扩展

唯一值得单独立项的是**概要节点 / 关联线**（内核零命中，需从零开发渲染 + 布局 + 导出）。
单项工作量约等于前几批总和，不建议顺手做。
